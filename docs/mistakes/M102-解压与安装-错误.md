# M102-解压与安装-错误

> 解压分派、防穿越、幂等与安装链路错误速查（一行一事，同根因合并聚合）。

## 行级条目

| 编号 | 日期 | 状态 | 现象 | 根因 | 正确处理 |
| --- | --- | --- | --- | --- | --- |
| M002 | 2026-09-01 | 已修正（2026-09-01） | mac 首跑 `cargo test` 后仓库 `catalog\tools.toml` 的 jq sha256 被改成 mac 资产哈希（A6FC..→2D75..），Windows 侧 pin 与 sha 失配 | tests\linux_install.rs 只沙盒了 HOME 与 env-root，漏了 catalog：OME_CATALOG 未设，catalog 四级解析按 cwd 命中仓库 catalog，install 的 sha 回写/滞后锁定落进仓库文件 | 集成测试沙盒三件套必须齐：HOME、env-root、catalog（OME_CATALOG 指向沙盒副本）；ome() 助手复制仓库 catalog 到 `<env_root>\tools.sandbox.toml` 再注入。跑测后以 `git diff catalog/` 为空作隔离断言习惯 |

## 范围注记

- 2026-09-01：该测试 `#![cfg(not(windows))]` 在 mac 首次真跑即暴露此坑（Windows 侧被 cfg、WSL 侧因 linux 字段被转换器吞而解析失败先行退出），属平台盲区型隔离缺陷。
