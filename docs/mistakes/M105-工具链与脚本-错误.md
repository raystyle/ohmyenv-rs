# M105-工具链与脚本-错误

> 工具链与脚本类错误速查（sed、grep、PowerShell、中文路径；一行一事，同根因合并聚合）。

## 行级条目

| 编号 | 日期 | 状态 | 现象 | 根因 | 正确处理 |
| --- | --- | --- | --- | --- | --- |
| M005 | 2026-09-02 | 已修正（2026-09-02） | AGENTS.md 计数更新（33→34）一条 PowerShell 管道后全文塌成单行、rumdl 报格式问题 | `(Get-Content f) -replace ... \| Set-Content -NoNewline`：`-NoNewline` 令 Set-Content 把**行数组无分隔拼接**为单行写出；AGENTS 规则 12 禁 sed 式批改中文 md 的同族坑（数组拼接语义而非正则歧义） | `git checkout -- f` 恢复后用 Edit 工具精确替换；中文与反斜杠路径的批改一律走 Edit 工具或 `.tools\md-replace.py`（规则 12），行数组写回禁用 `-NoNewline` |
