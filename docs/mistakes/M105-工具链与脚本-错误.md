# M105-工具链与脚本-错误

> 工具链与脚本类错误速查（sed、grep、PowerShell、中文路径；一行一事，同根因合并聚合）。

## 行级条目

| 编号 | 日期 | 状态 | 现象 | 根因 | 正确处理 |
| --- | --- | --- | --- | --- | --- |
| M005 | 2026-09-02 | 已修正（2026-09-02） | AGENTS.md 计数更新（33 改 34）一条 PowerShell 管道后全文塌成单行、rumdl 报格式问题 | `(Get-Content f) -replace ... \| Set-Content -NoNewline`：`-NoNewline` 令 Set-Content 把**行数组无分隔拼接**为单行写出；AGENTS 规则 12 禁 sed 式批改中文 md 的同族坑（数组拼接语义而非正则歧义） | `git checkout -- f` 恢复后用 Edit 工具精确替换；中文与反斜杠路径的批改一律走 Edit 工具或 `.tools\md-replace.py`（规则 12），行数组写回禁用 `-NoNewline` |
| M006 | 2026-09-05 | 已修正（2026-09-07 第四犯聚合） | 交叉编译失败 exit 0 假象（`cargo build 2>&1 \| tail -3` 吞了 error 细节）；heading-scan 两次只报一处违规（`\| tail -1` 每次只显示最后一行，修一处又冒一处）；2026-09-07 第四犯：`scan; echo $?; rumdl ok && git commit` 分号链里 scan exit 1 只被 echo 消化，提交照跑（S004 三处括号标题带病入库，补提交修复） | shell 管道退出码取**最末命令**（tail 恒 0），截断输出同时遮蔽错误与剩余违规行；分号链中前一命令失败不拦后续 `&&` 段：失败信号被链式结构吃掉是同根因四形态（tail 截断、tail 遮蔽、echo 消化、分号穿透），第五犯即本行聚合提交自身（mdcharlint 接 tail -1 恒绿放行） | 验证与构建命令**不接 tail**（全量输出）；**门禁与提交之间只用 `&&` 不用分号**（任一门禁失败必须拦住 git commit）；扫描类必须看全量输出与真实退出码；此纪律同时适用前台与后台任务（oma 仓同型两犯，见其 diary 2026-09-05 自省） |
| M012 | 2026-09-07 | 已修正 | ohmycloud#5 把 go/sops mac 派成补种，域面早已 200（v2.2 / #3 尾巴） | 差集只对上一份 ISSUE 文本，未对 `env.ohmygh.com` 做 HEAD | 派单前对拟补种/下架 URL 做域面 HEAD：已 200 不派补种，已 404 才派下架（R014） |
| M015 | 2026-09-10 | 已修正（2026-09-10） | `uv run --script .tools/seed.py --plan` 跑完，仓库工作树多出未跟踪文件 `catalog/tools.toml.sha256`（D32 typst 入册对账时发现，易被误提交） | 沙滚段上传 helper 把 `.sha256` 边车直接写进源资产所在目录（`local_asset.parent`）；版本目录与沙滚段两 helper 同病，而 catalog 本体的 parent 正是仓库 `catalog\` | 边车经临时目录暂存后上传：抽 `stage_sidecar` 加 `TemporaryDirectory` 包住上传循环（`upload_pair` 与 `upload_pair_seg` 同改），源资产目录一律只读不写；改动对域面对象与键名零影响（--plan 输出逐字不变） |
| M016 | 2026-09-10 | 已修正（2026-09-10） | D37 删权威件后 CI 三平台红：`tests/linux_install.rs` 仍 `fs::copy` 已删的仓库 `catalog/tools.toml`，ubuntu 与 macos 两 job 于该文件第 26 行 panic（exit 101）；Windows job 因整文件 `#![cfg(not(windows))]` 为 0 test 而绿，本机（Windows）`cargo test` 同样全绿，掩盖了它 | 两族同根因：一，删改公共/权威文件前未全仓搜引用（漏了 cfg 门控测试与 workflow 调用面）；二，**文件级 cfg 把整文件从本机编译面摘掉**，本机绿不等于矩阵绿（R004 三.6 已写每推 CI 三平台把关，属纪律执行漏跑） | 修复：`linux_install.rs` 取件源改动态解析（`OME_TEST_CATALOG` 到仓库件到用户数据副本到云端三重门），版本期望改由被测二进制自身输出；**只给 POSIX 行为用例挂 `#[cfg(not(windows))]`，助手代码全平台参与编译**，本机 `cargo test`/`clippy` 即可对它们把关。纪律：删改权威或公共文件先全仓搜引用；cfg 门控面必看 CI 三平台结果，本机绿不算数 |
| M024 | 2026-09-11 | 已修正（2026-09-11） | `.tools/tmp_fix.py`（行内改写脚本）删掉后又被下一笔提交加回仓，且内容是**旧稿**（写着已被替换掉的 `merge_env_exports` 版 fnm 钩子实现）| 一次性脚本没地方「归档」，改完顺手 `git add -A` 就带进来；删过的东西没人盯着回仓 | 一次性脚本用完即弃（`.tools` 规则 1：纯一次性的留对话不进仓）；提交前看 `git status` 的 `??` 与新增文件；二犯即升格纪律（本行）|
| M020 | 2026-09-11 | 已修正（2026-09-11） | 给 `tests/install.rs` 加集成用例后，`git diff --stat` 报该文件 137 增 87 删（整文件近乎全改），实际只加了 50 行 | 编辑工具在本就 CRLF 的行上写入 LF，文件变成 mixed EOL（`git ls-files --eol` 显示 `w/mixed`）；本仓无 `.gitattributes` 且 `core.autocrlf=false`，于是 EOL 差异被算成整文件改写，真改动被淹没 | 改完立即三查：`git diff --stat` 是否与预期行数量级相符、`git ls-files --eol <file>` 是否仍单一口径、异常时按仓内主流行尾整文件归一（本仓 LF）。根治选项：加 `.gitattributes` 定 `* text=auto eol=lf`（需全仓归一评估，另行裁） |
| M018 | 2026-09-11 | 已修正（2026-09-11） | 删 `extract::ensure_bunx_shim` 时按行盲切，把相邻的 `zip解压_含目录与嵌套文件` 用例一并删掉（工作记录写「删三测试」，可 lib 用例总数 107 掉到 103 也没对上账）；同一文件还两次括号失衡 | PowerShell 正则或按行号切带 `mod tests` 的 Rust 文件，切点落在相邻用例之间；验收只看「双端绿」不看用例总数基线，绿的集合小了照样绿；括号失衡是同一个盲切的伴生症状 | 删函数段用 AST 感知工具或精确替换，不用行号盲切；删完做用例总数基线比对（`cargo test` 条数或 `ast-grep outline -l rs` 符号表前后 diff）；切后必跑 `cargo check` 加 `ast-grep outline` 复核，别靠肉眼看括号 |
