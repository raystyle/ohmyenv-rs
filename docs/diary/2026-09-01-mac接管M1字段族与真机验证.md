# 2026-09-01 mac 接管：M1 字段族与真机验证

> 本机 mac 首个开发批次（R011 流程首跑）：M1 mac 专属字段族落地 + R011 六项真机验证全绿 + ohmypwsh 已部署工具幂等接管实证。

## 做了什么

1. **真机门禁首跑暴露三处真问题并全部修复**：黄金 status 测试失败（`toolver::exe_path` 非 Windows 分支对回退通用 exe 双拼 dir 段，`age/age/age.exe`）；clippy 13 错（vsbuild/extract 的 Windows 专属代码无 cfg 门控、两个纯 Windows 测试 mod 的 `use super::*` 闲置）；`tests\linux_install.rs` 在 mac 首跑即把 jq 的 mac 资产 sha 回写进仓库 catalog（沙盒漏 catalog，记 M102/M002）。
2. **M1 代码**：`Tool` 增 `mac_*` 专属字段族（与 linux 族对称，含 `mac_cdn_url`），`effective_*` 访问器改三分支回退链（mac、linux、通用依次回退，`target_os = "macos"` 门控）；`platform_exe()` 暴露专属 exe 原始值支撑 exe 双语义；`join_if_relative` 上移 platform 复用。
3. **数据**：jq/fnm 首批 mac 补录（jq-macos-arm64 裸二进制 copy + sha256sum.txt 官方校验；fnm-macos.zip universal 单二进制 zip），R001 同步 mac 族契约与 exe 双语义。
4. **R011 六项真机验证全绿**：build/test 70 项、install/deploy jq 幂等、self-deploy 部署态 catalog 独立解析、package fnm 输出 `fnm/bin/fnm`、windows-gnu 交叉 check（装 mingw-w64 后 22.75s 通过）。
5. **幂等接管实证**（用户明确要求）：本机 `~/.local/bin` 已有 ohmypwsh 部署的 26 个二进制；`ome install jq --latest` 对在位 jq-1.8.2 报 skipped 零重装、`deploy` 对 `.zshrc` 已有同文 export 零改写、仓库 catalog 全程零改动（`git diff catalog/` 为空作隔离断言）。

## 自省

- **平台盲区型缺陷靠真机首跑才能暴露**：`#![cfg(not(windows))]` 的测试在 Windows 基线全绿（被 cfg）、在 WSL 因数据缺失先行退出，唯独 mac 真跑才走到回写分支：非 Windows host 的门禁（test + clippy）必须成为每批必跑项，R011 验收标准已补记。
- **沙盒纪律**：集成测试沙盒必须三件套齐（HOME、env-root、catalog），缺一即静默污染仓库；本次靠跑后 `git diff` 兜住，已固化为习惯断言。
- M0 注意：转换器「只校验不再生」改造时，`mac_*` 字段（jq/fnm 已有）与 linux 字段同列保护，防止再吞。

## 下一步

M0 数据主权与回流（psd1 Pos 侧 26 工具 linux pin 回流、import-catalog.ps1 改只校验、go/zig/shellcheck/kimi 补录）；mac 侧逐工具 `mac_*` 补录随 M0 数据到位分批推进（pwsh 等 status 失真项优先）。
