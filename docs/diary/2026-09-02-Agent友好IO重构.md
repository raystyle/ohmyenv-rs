# 2026-09-02 Agent 友好 IO 重构

> 当天做了什么（项目日记，按日期归档）。

## 研究与重构

- **docs/research/S003**：用户指定三家 oracle 做 Agent 友好 IO 代码级研究——gh 官方手册（--json 字段选择/--jq/默认值入类型/错误恒 stderr/空列表 []）、git clone 官方手册（进度恒 stderr/TTY 条件进度/--[no-]xxx 否定/SYNOPSIS）、incurs clone 源码（Formatter 五格式同源渲染/--json 简写/错误尊重格式/ValidationError 逐字段/--filter-output/--token 分页/--llms/--schema/TTY 判别 agent）。吸收裁决与不吸收理由全部落 S003 表格。
- **feat/**：全局 `--format kv|json|jsonl` + `--json` 简写（互斥报错）；render.rs 三态渲染层（kv 逐行、json 整批数组 stdout 恒合法文档、jsonl 逐块单行流式）；结构化错误走 stderr 单行 JSON（stdout 保持纯数据，daily exit 2 时已产出预览行照常上 stdout）；verify/doctor 子命令级 --json 退役统一全局，kv 形状零破坏（ohmypwsh 正则消费不回归），结构化出富字段（verify name/verdict、doctor check/status/detail）；serde_json 开 preserve_order（字段序与 kv 行序一致）；`bin_name = "ome"`（Windows usage 不再显示 ome.exe，用户点名）。
- **测试**：render 纯函数四测 + cli.rs 集成五测（json 数组无 # 标题、--json 简写、jsonl 逐行、结构化错误、互斥）；全量 cargo test 91 项全绿。
- **README**：新增「输出格式与退出码」节（三格式语义、九命令字段清单、退出码 0/1/2）。

## 四远端验收

- 用户指示：linux 验收用 WSL，mac 走 `ssh ray@lan-mac`，lan 走 `ssh ray@lan-linux`、`ssh ray@lan-win`，或配合 ohmypwsh 同步链。
- **实证五端全绿**：本机（init 自部署，verify jsonl、status json）、WSL（pull+原生构建，注意 init 复制的是运行中 exe，须以 `target/release/ome init` 部署新构建）、lan-linux（ohmywsl Sync-OmeToRemote sha 对比按需传）、lan-win（sync-ome-lanwin 下发）、lan-mac（pull+构建+init，部署位与构建产物 sha 一致）。
- ssh 主机别名走 config 权威（lan-win/lan-linux/lan-mac 已含 HostName 与 mesh key）；ohmypwsh sync-ome-lanwin 默认目标由 IP 改别名 `ray@lan-win` 并去掉强制 key 覆盖。

## 流式补全——用户 review 后第二批

- **feat/**：doctor 流式化——`run_doctor_with` 回调形态（envroot 项在三态采集前即出，采集完连出五个派生项，PATH 两项与缓存孤儿随后；run_doctor 保留为空回调兼容口）。
- **feat/**：daily 流式化——`run_daily_with` 回调形态（解析逐工具走网络，判定行逐工具即出；run_daily 保留兼容口）。
- **review 结论**：其余命令（query/pin/install/deploy/update/package）本就是逐工具循环内即出，天然流式；status/verify 已回调化。至此全部数据命令流式。

## 环境

- rustc 1.97.1 / windows msvc；基线 379bc1a 加本日提交
