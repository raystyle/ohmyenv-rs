# 2026-09-02 Agent 友好 IO 重构

> 当天做了什么（项目日记，按日期归档）。

## 研究与重构

- **docs/research/S003**：用户指定三家 oracle 做 Agent 友好 IO 代码级研究——gh 官方手册（--json 字段选择/--jq/默认值入类型/错误恒 stderr/空列表 []）、git clone 官方手册（进度恒 stderr/TTY 条件进度/--[no-]xxx 否定/SYNOPSIS）、incurs clone 源码（Formatter 五格式同源渲染/--json 简写/错误尊重格式/ValidationError 逐字段/--filter-output/--token 分页/--llms/--schema/TTY 判别 agent）。吸收裁决与不吸收理由全部落 S003 表格。
- **feat/**：全局 `--format kv|json|jsonl` + `--json` 简写（互斥报错）；render.rs 三态渲染层（kv 逐行、json 整批数组 stdout 恒合法文档、jsonl 逐块单行流式）；结构化错误走 stderr 单行 JSON（stdout 保持纯数据，daily exit 2 时已产出预览行照常上 stdout）；verify/doctor 子命令级 --json 退役统一全局，kv 形状零破坏（ohmypwsh 正则消费不回归），结构化出富字段（verify name/verdict、doctor check/status/detail）；serde_json 开 preserve_order（字段序与 kv 行序一致）；`bin_name = "ome"`（Windows usage 不再显示 ome.exe，用户点名）。
- **测试**：render 纯函数四测 + cli.rs 集成五测（json 数组无 # 标题、--json 简写、jsonl 逐行、结构化错误、互斥）；全量 cargo test 91 项全绿。
- **README**：新增「输出格式与退出码」节（三格式语义、九命令字段清单、退出码 0/1/2）。

## 四远端验收

- 用户指示：linux 验收用 WSL，mac 走 `ssh ray@lan-mac`，lan 走 `ssh ray@lan-linux`、`ssh ray@lan-win`，或配合 ohmypwsh 同步链。
- **实证五端全绿**：本机（init 自部署，verify jsonl、status json）、WSL（pull+原生构建，注意 init 复制的是运行中 exe，须以 `target/release/ome init` 部署新构建）、lan-linux（ohmywsl Sync-OmeToRemote sha 对比按需传）、lan-win（sync-ome-lanwin 下发）、lan-mac（pull+构建+init，部署位与构建产物 sha 一致）。
- ssh 主机别名走 config 权威（lan-win/lan-linux/lan-mac 已含 HostName 与 mesh key）；ohmypwsh sync-ome-lanwin 默认目标由 IP 改别名 `ray@lan-win` 并去掉强制 key 覆盖。

## 流式补全——用户 review 后第二批

- **feat/**：doctor 流式化——`run_doctor_with` 回调形态（envroot 项在三态采集前即出，采集完连出五个派生项，PATH 两项与缓存孤儿随后；run_doctor 保留为空回调兼容口）。
- **feat/**：daily 流式化——`run_daily_with` 回调形态（解析逐工具走网络，判定行逐工具即出；run_daily 保留兼容口）。
- **review 结论**：其余命令（query/pin/install/deploy/update/package）本就是逐工具循环内即出，天然流式；status/verify 已回调化。至此全部数据命令流式。

## ome 自升级第三批

- **feat/**：`ome self update`（selfupdate.rs）三通道——**dev** 默认（CI push main 构建滚动挂 pre-release `dev`）、**--stable**（v* tag 封版触发 releases/latest）、**--git**（封版前源码安装：浅克隆 cargo build 后替换，需 git 与 cargo）。升级判定用 release 资产 API digest（sha256）对比运行中 exe 的 sha256，一致 current 跳过，不同则下载校验替换部署位（Windows 改名旧 exe 为 .old 再 copy 回滚保护，Unix chmod 755 原子 rename），并刷新数据目录 catalog（release 通道走 raw.githubusercontent，git 通道用克隆内文件）。用户裁决：正式 release 由版本 tag 触发、dev 滚动源由 main 推送触发、封版前用 --git。
- CI `.github/workflows/build.yml` 双通道路由：push main 构建测试后资产滚动挂 dev；push v* tag 构建测试后资产挂正式 release。三平台 windows-x64 msvc、linux-x64 gnu、macos-arm64（用户裁决 mac 不需要 intel）。
- 直连 api.github.com 带 GH_TOKEN 注入，403/限流回退 gh api（对齐 resolve.rs 模式）。
- **CI 首跑抓两坑**：Windows 归位资产名 `cp ome*` 误匹配 ome.pdb（改按 matrix.bin 精确名）；unix 测试挂 `verify::tests::文件组判定`——测试用 Windows 反斜杠路径跑在 unix（运行时 DIMS 在 unix 全是 `~`/`/` 前缀无此问题，纯测试缺陷，潜伏到 CI 才暴露），改 MAIN_SEPARATOR 构造。

## WSL msi 接管第四批

- **feat/**：catalog 增 `wsl` 条目（microsoft/WSL，tag 无 v 前缀，pin 2.7.12 + 缓存文件回填 sha256，无官方 sums）。三个配套改造：
  - toolver `decode_output`：UTF-16LE 无 BOM 输出按 NUL 密度启发式解码（实证 `wsl --version` 字节序 W,0,S,0,L,0——from_utf8_lossy 会插 U+FFFD 致正则全灭）；
  - wsl 版本正则 `(\d+\.\d+\.\d+)(?:\.\d+)?`：输出四段（2.7.12.0）归一三段对齐 tag；
  - extract msi 分支加 gsudo 提权（未提权且 gsudo 在位时 `gsudo msiexec`，对齐 vsbuild 模式）。
- exe 建模 `%ProgramFiles%\WSL\wsl.exe`（msi 实体，System32 是 inbox stub，FileVersion 10.x 不可用作版本源）。
- 实证：status 三态 locked=installed=2.7.12、query 解析直链、install 幂等跳过（探测归一命中 pin）、74 测试全绿。

## Docker Engine 接管与 unix 测试修复第五批

- **feat/**：catalog 增 `docker` 条目（download.docker.com static zip，CDN 直链 pin 驱动 dotnet/oscdimg 同族，sha 本地回填 B40782FF...）；`docker.rs` 专用模块完整迁移 set-docker.ps1——static zip 解压 docker/dockerd、docker-users 组、daemon.json 合并（serde_json preserve_order 保键序、保留用户自定义键）、Windows 服务注册与旧指向卸载、compose 插件（v5.5.0 官方 .sha256 校验）、~/.docker/config.json cliPluginsExtraDirs、机器级 PATH、gsudo 提权重跑（vsbuild 同模式）。真机三态 locked=installed=29.7.1（set-docker.ps1 既有安装被无缝接管），install 幂等 skip。
- **fix/**：CI unix 测试三连修——①verify 文件组测试 Windows 反斜杠路径跑 unix（MAIN_SEPARATOR 构造）；②fixture 缺 linux/mac 布局字段致 pin 视图走跳过分支（补三平台布局，posix 用沙盒相对目录保确定性——~/.local/bin 会探到真机已装工具）；③normalize 增 HOME 归一 + status 增 linux/macos oracle（linux WSL 真机生成、mac 同构推导待核对）；pin.linux.txt oracle 真机核对通过。WSL 与 Windows 双平台全量测试绿。
- **chore/**：oma init 脚手架落仓（CLAUDE.md AGENTS.md 指针入库，六个 agent 目录入 .gitignore，照 ohmypwsh 惯例）。

## self update 五端闭环第六批

- **实证**：CI 全绿后 dev release 三资产带 API digest 就位；本机 `ome self update` 闭环（第一次 updated：digest 对比、下载校验、替换部署位、catalog 同步；第二次 current 跳过）；四远端以 CI 资产铺种子（WSL/lan-linux/lan-mac curl 直下、lan-win Invoke-WebRequest）后 `ome self update` 全部 action=current 且 sha 与 dev 资产一致。自此五端 ome 升级自服务，ohmypwsh 侧二进制下发链（Sync-OmeToRemote/sync-ome-lanwin）完成历史使命可退役（归 ohmypwsh 侧裁决）。

## reader 与 browser-harness 接管第七批

- **feat/**：reader 升 0.2.1 并补三平台（linux targz-bin + mac arm64，官方 .sha256 后缀校验，三平台 sha 手工核对入库）；本机 0.1.0 升 0.2.1 实测。
- **feat/**：browser-harness（agent 操控真实浏览器）新条目——`uv tool install git` 安装型（无预编译资产）：resolve 第四分支 uv-git（pin 驱动，无 latest 解析，升级改 catalog pin）；`install_uv_git` 依赖 uv 运行时（不在位提示先 ome install uv）；exe 以 `%UV_TOOL_BIN_DIR%` 环境变量展开（实测 shim 落 D:\ohmyenv\uv-tools\bin，UV 重定位 EnvRoot 的结果）；`--version` 输出裸版本号（0.6.6）。
- **M102 吸收（用户指引研究 browser-harness 自升级方案）**：Windows 运行中进程锁 venv Scripts\，uv --force 删目录报拒绝访问（os error 5，本机实测 PID 占用）；其官方 run_update 的解法是升级前 `_stop_stack_for_upgrade`（rmux server kill + daemon restart_daemon 停止）+ Windows 中继 shell（父进程退出后再装）。ome 吸收为：升级路径（旧 exe 在位）先跑工具自身 CLI `--reload` 与 `rmux kill-server` 解锁（best-effort），装后 HINT 提示 `browser-harness x-monitor` 恢复值守栈（ome 不擅自拉起）。
- **待办**：`--force` 升级停栈路径实测未跑（会停用户在用的守护栈，等合适时机）；oma init 的 pretrust 未生效（trust.project block），ohmyenv-rs 目录信任弹窗需用户接受一次。

## 会话交接——2026-09-02 下午

- 用户裁决：**D:\ohmyenv-rs 是本项目目录**，新会话在该目录拉起（yolo 生效需接受一次 folder trust）；ohmypwsh 归另一 agent 会话接管开发。
- 本日批次：S003 Agent 友好 IO（三格式/结构化错误/流式补全）五端验收、self update 三通道与 CI 双通道、WSL msi 接管、Docker Engine 接管、unix 测试修复、reader/browser-harness 接管。基线 c1a6aa6。

## M4 heal 移植第八批

- **feat/**：`ome heal [<dim|all>] [--dry-run]`（heal.rs）——heal-map.psd1 的 42 键迁嵌入注册表（44 行，toolRoot/aria2 按平台分列），四类归宿：
  - **install 类原生安装**（toolRoot/bun/dev-go/dev-zig/dev-fnm/dev-rust/vsbuild/aria2/go/zig/localbin16/rmux/uv/astgrep/yq，pin 驱动幂等，vsbuild/docker/rustup 特型分派与 cmd_install 同源；localbin16 = install all 对齐 ohmywsl）；不注册 PATH、不回写锁定（heal 只修到 verify 可过，对齐 heal-map 的 ohmyenv install 语义）；
  - **dsKey/akKey 与 bunfig/goproxy 原生移植**——heal-keys.py（POSIX env.sh 载体逐字节内容 + 内容时效四标志校验 + 三 rc 挂钩；Windows ANTHROPIC_BASE_URL 端点，get_user_env_var 新增）与 heal-mirror.py（bunfig npmmirror 双平台、goproxy 仅 POSIX，Windows 由 go env -w 管理）；
  - **agent 域 12 键休眠**（A/B/H 族 + residOldsg，2026-09-01 裁决，只提示不执行）；**非 ome 域 8 行路由提示**（sgPy/sgRef/win-sg* 密钥防护域、residOld* set-* 脚本族、compileMatrix 验收编排、POSIX aria2 系统位）；
  - **mac-* 4 键别名归一**（ome 在 mac 本机原生跑普通键，ps1 远端路由时代的专列退役）。
- **测试**：单测 42 键名与 psd1 oracle 对账、verify 维度全覆盖（fail 可自愈的注册表约束）、bunfig/goproxy/env.sh 沙盒幂等（POSIX 载体测试 unix 跑）；集成 7 项（未知维度 exit 1、休眠/路由/不适用提示、dry-run 计划行、json 结构化、mac 别名 posix 跑）。全量测试双平台绿。
- **验收闭环（双平台）**：Windows aria2（工具缺失：改 exe 名 → verify FAIL → heal 命中缓存重装 → PASS）、bunfig（镜像源：去 npmmirror → 重写恢复）、dsKey（密钥载体：删 ANTHROPIC_BASE_URL → 恢复）、claude（agent 休眠演示）；WSL go（破坏 exe → heal 缓存重装 1.27.0 → PASS）、dsKey（真机 env.sh 与三 rc 已挂钩状态全 ok 零改写，POSIX 移植与 ohmypwsh 时代产物逐字节兼容）；`heal all` 两连零 diff（13 维全 ok）+ verify 9 维全 PASS。
- **平台语义备注**：POSIX aria2 维度验系统位（apt /usr/bin、brew /opt/homebrew），ome 绿装域无法自愈，键路由提示（与 ohmypwsh heal-map 单键本机执行的实际效果一致）；`uv` 键保持 POSIX 专列（Windows 侧 uv 属 install all 域）。

## rust 接管与 dev-rust 转原生第九批

- **feat/**：`rustup.rs` 专用模块完整迁移 set-rust.ps1——catalog 增 `rust` 条目（rustup 引导器型 `extract = "rustup"`，rsproxy 直链 evergreen 无 pin，stable 滚动即更新语义）；安装六步：四用户环境变量（RUSTUP_DIST_SERVER/RUSTUP_UPDATE_ROOT 镜像 + RUSTUP_HOME/CARGO_HOME 重定位 EnvRoot，比较后写并同步进程内供子进程消费）→ rustup-init 引导（rustc 在位跳过，`-y --default-toolchain stable --default-host x86_64-pc-windows-msvc --no-modify-path`）→ update/default stable 保最新（失败 WARN 不拦，末尾版本校验兜底——对齐 ps1 原生命令非零不抛的行为）→ cargo sparse 镜像 config.toml（逐字节内容比对）→ cargo bin 用户 PATH → rustc 版本校验。
- **接线**：query/pin（evergreen 语义块）、install（rustup::install）、update/daily（evergreen 跳过）、doctor（pin 缺失豁免）、heal dev-rust 键由路由转原生 Install（install_one 特型分派）；toolver 增 rust 正则（`rustc (\d+\.\d+\.\d+)`）。
- **语义备注**：cargo bin PATH 尾部追加（ome 全仓惯例）而 set-rust 为前置——已装机器由 ps1 前置位保持不变，全新机器仅一个 cargo bin 在 PATH 无遮蔽差异。
- **真机验收**：本机 set-rust.ps1 时代安装被无缝接管（环境变量四键已位 INFO、rustc 在位跳过 init、update stable 滚动到新工具链、config.toml 一致、PATH 已含），status 增 rust 行，heal dev-rust → install rust 幂等。

## bun mac 建模缺口修补第十批

> 对应本仓 issue #2（mac 端 bun 建模缺口）。

- **catalog/**：`[tools.bun]` 补 mac 字段族——ohmypwsh mac 批次验收发现 lan-mac bun 维 1/7 FAIL（无 mac 字族致无 pin 无探测规格，M1 mac pin 补齐批次遗漏项）。`mac_asset_pattern = bun-darwin-aarch64.zip`、`~/.local/bin` 平铺（与 linux 同构：darwin zip 顶层目录平铺，extract zip 共享目录 flatten 路径已实证）、pin 1.3.14 对齐 linux 锁定（hold 裁决跨平台同锁）；sha 取官方 SHASUMS256.txt 回填（d8b96221...，与既有 linux/windows pin 同源核对逐字一致）。
- **验证**：本机 catalog 解析与 windows 三态不受影响（query bun 正常）；fixture 测试全绿（81+21）。**mac 真机复验闭环（未推送，catalog 数据目录直达）**：`~/Library/Application Support/ohmyenv/catalog/tools.toml` 同步新文件后 verify 仍 FAIL——实证 issue 里「exe 在位」系误读（status 的 exe 行是期望路径，`~/.local/bin/bun` 从未存在），`ome install bun` 一次通过（官方 sha 实证一致、平铺、bunx shim、hold 装锁定版 1.3.14），**verify 收 7/7、ohmymac verify 薄转发链同结果 FAIL 0**，已回帖 issue #2 可关闭。ohmypwsh 侧 import-catalog.ps1 已随其 M6 收缩退役，平台族完整性以本仓 catalog 为唯一权威。

## 推送与 CI 限流修复第十一批

- **推送**：3b00506..32a9bc7 四笔（M4 heal、rust 接管、bun mac、diary）上远端；issue #2 复验达成后关闭。
- **fix/CI**：push 后 macos job 两测试挂（`linux_jq_安装部署状态闭环`、`linux_profile_path_幂等`）——真网测试打 `api.github.com/repos/jqlang/jq/releases/latest` 匿名请求，共享 runner 出口 IP 限流 60 次/时（此前 aad6d13 那轮失败同源，flake 非代码回归）。双端修：resolve.rs `http_get_json` 对 api.github.com 读 `GH_TOKEN`/`GITHUB_TOKEN` 注入 Bearer（对齐 selfupdate 模式，gh api 兜底同认它）；build.yml 测试步补 `GH_TOKEN: secrets.GITHUB_TOKEN`。
- **五端 self update 闭环**：CI 全绿（a2b3d8c）后 dev 资产滚动，五端全部 `action=updated` 且 catalog 同步——本机与 lan-win 240B01E6（windows-msvc）、WSL 与 lan-linux 2C20A6F7（linux-gnu）、lan-mac 2D65822B（darwin）；mac/WSL 实证新二进制 heal 命令在位（dry-run 计划行），mac verify 保持全 PASS。lan-win 非交互 ssh 会话 PATH 不含 ome 需全路径调用（cmd 默认 shell，记备）。

## query 契约冻结与 package 批量容错第十二批

> 对应本仓 issue #4（对外数据契约）。

- **feat/契约**：query 增 `sha256` 字段（issue #4 请求的 version/asset/url/sha 四要素补齐）——解析 tag 与资产同 pin 时给锁定 sha256（未回填空串），否则空串；`--format json` 字段集与退出码（0/1）冻结为对外契约，README 落增量字段非 breaking、删改即 breaking 的演进规则。ohmypwsh 镜像链（build-wsl-image / download-posix-dev 替代）可切 ome query 数据源。
- **feat/package**：all 批量容错——安装器型（vsbuild/rust）与平台不适用在 all 循环跳过续跑（skipped 行加 WARN），单工具显式调用仍即失败（对齐 install all 语义）；sha 校验本就与 install 同口径（expected_sha256 加 download_asset），issue 验收 3 天然满足。
- **测试**：query sha256 同 pin 离线断言（fixture 平台 pin sha 前缀）；全量 82+22 绿。
- **issue #3 回帖**：M4 完成证据矩阵（四验收目标逐条）、agent 键休眠与剔除的口径差异说明（P0026 原文即休眠，语义等价零执行）、五端已 self update 可接线；lan-linux/lan-win/WSL 新二进制 yq 键幂等两连 ok 补证。

## doctor 误报修正与 OpenSSH 接管第十三批

- **fix/doctor**：两类误报修正——①sha-missing 豁免 uv-git 型（uv tool install git 源无下载资产，sha 不适用，browser-harness 误报消除）；②cache-orphans 放行派生资产（catalog bootstrap_asset 清单如 7zr.exe、vsbuild/rustup 引导器、docker-compose 前缀族、ome 自升级资产——被安装链消费但不属任何 pin）。各配纯函数单测。
- **feat/openssh**：Windows OpenSSH 接管（用户裁决「ohmyenv 需要接管」）——catalog 增 `openssh` 条目（PowerShell/Win32-OpenSSH MSI 型，pwsh/wsl 同族：gsudo msiexec 提权、MSI 自管 PATH 与服务）；本机既有安装（tag 10.0.0.0p2-Preview、运行时 10.0p2、sshd/agent 双服务运行中）无缝接管——install 幂等跳过零打扰，status 三态 locked=installed=10.0p2；sha 自缓存 MSI 本地回填。
- **fix/resolve（通用语义加固）**：pin 驱动解析（无 latest/tag/version 选项）时 version 取 pin 四键的显式 version 而非从 tag 推导——tag 形态不规则的条目（openssh 的运行时 10.0p2 与 tag 10.0.0.0p2-Preview）此前推导出错误期望版本致幂等跳过失效；对所有既有条目等价（pin 与推导本就一致）。
- **toolver**：openssh 探测 `ssh -V`（stderr 输出，版本正则 `OpenSSH_for_Windows_([\d.]+p\d+)`）。
- **清理（本机态）**：删用户 PATH 死链 `D:\ohmyenv\gopath\bin`（go\bin 在位的前置已核，保 REG 类型写回）；删 7 个真孤儿资产 492MB（agent 四件套残留 claude/codex/grok/kimi、旧版 reader 0.1.0、旧 7z sfx、Reasonix zip）。doctor 终态 9 项 FAIL 0 WARN 0。
- **记备**：`D:\ohmyenv\reasonix` 在 PATH 且按 EnvRoot 布局安装但**不在 catalog**（未接管工具，用户手动部署）——待用户裁决是否建模入册。

## 七类 taxonomy 与 ome 自管条目第十四批

- **catalog/**：36 节全量归入七类（用户四轮裁决定稿，统一「依赖」后缀）——**操作编排依赖** base（ome/oma/herdr）、**运行时依赖** runtime（pwsh/python/dotnet/bun/fnm/wsl/docker/nushell）、**编译器依赖** compiler（go/zig/rust/vsbuild）、**运行时衍生依赖** derived（browser-harness，经 uv tool 安装；omcf 待 ohmycloud）、**多路复用依赖** mux（rmux）、**远程服务依赖** service（openssh/vault，客户端加服务端常驻）、**命令工具依赖** cli（其余 20）；节序按类序重排（保留注释的一次性 py 脚本切片重写，跑完即删）。
- **feat/ome 自管条目**：`extract = "ome-self"`（category=base 组首）——无 pin 无资产，探测位 init 自部署目标，query/pin/install/update/package/daily/doctor 全 evergreen 式特型（提示 `ome self update` 三通道）；clap 增 `version`（`ome --version` → 0.1.0），toolver 增 ome 正则；部署位旧 exe 无 --version 致 probe-fail WARN 属预期，self update 后自解。
- **status/**：两层分组（核心/扩展 + 子类）收敛为**七类单层组头**（GROUPS 注册表驱动，节序即类序的变更检测流式出组）；旧 category 值兜底转换期防炸。
- **测试**：fixture 三工具改七类并按类序重排（python runtime→vault service→age cli）；黄金五件全量重生成（status/pin × win/linux/macos，linux 于 WSL 真机核对过）；catalog 保序断言、七类 GROUPS 断言、cli 组头断言更新。双平台全绿。
- **记备**：oma（ohmyagents CLI，cargo bin 装位、无 --version、dev tag 三平台资产加 .sha256 齐备）入操作编排依赖组待 ohmyagents 侧补 --version 与通道裁决——拟发 issue；omcf（ohmycloud CLI，npm-link 本地 0.0.1）入运行时衍生依赖组待 ohmycloud 就绪。工具计数修正：旧正则漏数字开头节名（7z），实际 36+ome=37。
- **顺带**：doctor 误报修正与 openssh 接管见第十三批（同日）。

## 写作规范转换与全量禁字符清剿第十五批

- **docs/G001 v2**：用户提供《中英文 Markdown 技术文档写作规范》v1.0，全文吸收为项目规范（用户裁决「转成项目规范和规则」）：四类禁用字符硬禁令（破折号与连接号、Unicode 箭头、emoji 与装饰符号、白名单外全角与智能引号）、豁免区（围栏/行内代码、链接目标、裸 URL、引文原文、规则定义区）、替代写法表、空格混排与结构规则、GFM 警示块、输出前自检；历史归档豁免（diary 与 proven 不回改，新写作含新 diary 批次一律合规）。
- **tools/mdcharlint.py**：规范随附校验脚本落地 `.tools`（PEP 723 头、argparse 化、默认跳过 diary/proven，`--all` 全量），进门禁为文档验证四件套（AGENTS 规则 3/9/项目工具路由、`.tools\README` 清单同步）。
- **存量清剿**：非归档区 105 处全修（破折号改冒号/括号/分号/拆句、箭头改文字与顿号列举、哈希变化括号化；R001/R010/R011/R012/S002 标题去破折号改逗号式，G003/G004/AGENTS/三原语/README/INDEX/M102/M105/R004/R009/S003 逐处替换）；oma init 脚手架四份 SKILL.md 副本同步修（模板源头在 ohmyagents，拟随 oma 集成 issue 一并提）。四门禁全绿（rumdl、mdcharlint、断链、标题括号）。

## 环境

- rustc / windows msvc；基线 379bc1a 加本日提交。第九批验收途中活工具链被 `rustup update stable` 就地滚动 1.97.1 → 1.98.0（ome install rust 自己触发的 update），新 clippy 带 chunks-exact-to-as-chunks 新 lint，checksum.rs 与 toolver.rs 两处改 `as_chunks::<2>()`（1.98 稳定；CI 用浮动 stable 同步就位）。rustup 自更新（1.29.0→1.29.1）报 os error 2，WARN-继续路径实证。
