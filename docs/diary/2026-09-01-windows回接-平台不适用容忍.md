# 2026-09-01 Windows 回接平台不适用容忍

> 当天做了什么（项目日记，按日期归档）。

## 工作记录

- **chore/回接**：按 R011 一.4 要点拉回 mac 三批次 10 笔提交（`e53e806..9d86f26`），Windows 基线门禁全绿：cargo test 76 项（Windows cfg 回归）、fmt/clippy 零告警、md 三件套零问题、转换器校验器「托管 26 节与 psd1 一致、本地节 5 个不校验」（实证）。
- **fix/平台不适用容忍**：真机复验撞出 Windows 回归——`ome status` 退出 1 报「工具缺少 exe 字段」。根因：M0 补录的 shellcheck 只有 `linux_*` 字段族无通用 `exe`，Windows 侧 `exe()` 回退链不达（mac 走 mac→linux→generic 三分支不受影响；测试全走夹具 catalog 未暴露；真机闸门 OME_TEST_REAL 未跑 status）。修复：`toolver::platform_managed`（effective exe 存在即本平台在管）落三层语义——status 出如实空态行（`installed=-`、`exe=-`）、install/update/daily/pin/query 跳过（stderr INFO 加 action=skipped 行）、package 拒绝（exit 1）；`StatusRow.exe` 改 `Option`。附跨平台可移植用例（全平台无 exe 的 ghost 工具，mac/Linux 侧同样成立）。
- **实证**：76 测试全绿；真机 `ome status` exit 0、31 工具全列、shellcheck 空态行；`query shellcheck` 跳过 exit 0；`package shellcheck` 拒绝 exit 1（管道早停会假读 exit 码，测退出码勿接 `Select-Object -First`）。

## 关键结论

- 平台字段族的「单平台工具」是常态数据形状（shellcheck 仅 linux、aria2/git 仅 Windows），代码层凡消费 effective 字段处必须容忍缺失（实证，M106 记 M003）。
- mac 三批次未暴露此问题的原因链：exe 回退链平台不对称（mac 有 linux 兜底、Windows 无）加测试夹具三工具全带通用 exe（实证）。

## 幂等接管 review 与两修

- **review/软件清单幂等接管**（用户指令，除 agent）：31 工具三态对照——26 个完美接管（locked=installed、path=true，jq/bun 实证 install 零改写 catalog）；其余 5 个语义可解释：vsbuild（evergreen 无 pin 设计）、shellcheck（Windows 平台不适用）、pwsh（msi 型，PATH 由安装器管机器级，ome 如实只报用户级）、go 与 zig（已装已探测，缺 Windows pin）。
- **fix/gh 漂移与展平误判（M102 M004）**：review 暴露 gh 装的是 2.97.0 而 pin 2.98.0；`ome install gh` 装后验证失败且旧目录已被清（删旧重建流）。根因：gh 2.98.0 起 windows zip 无包裹层（顶层即 bin/gh.exe 与 LICENSE），展平宽松模式（唯一目录加允许根文件，为 Linux 共享目录设计）把业务目录 bin/ 误当包裹层。修：展平按平台分口径——Windows 专属目录严格判定（唯一子目录且零文件），宽松仅留 Linux/mac；附 gh 新布局回归测试。复装成功，gh 2.98.0 在位。
- **feat/hold 版本锁定**（用户裁决：bun 1.3.14 是最后一个完全用 Zig 编写核心的版本，锁定）：catalog 静态字段 `hold = true`（跨平台），update（含 --force）/daily/pin/带选项 install 全跳过并提示解锁方式，无选项 install 仍按 pin 幂等；R001 补字段与语义，bun 条目落 hold。真机四命令面验证过。
- **缺口登记**：go/zig 的 Windows 安装建模缺——golang/zig 不走 GitHub Releases（go tag 404 实证），需按 mac 模式建 cdn_url 直链解析（go.dev/dl、ziglang.org/download）加 zip-dir 提取型（Windows 资产是 zip，现有 dir 型只有 targz/tarxz）；已登 TODO。

## go/zig Windows 建模补齐与遥测关闭

- **feat/go 与 zig 补齐**：golang/ziglang 不走 GitHub Releases（golang tag 404 实证），Windows 侧照 mac 模式建 cdn 直链——go 补 `cdn_url`（go.dev/dl，zip 型：顶层唯一 go/ 包裹，严格展平恰好正确）与 dir；zig 补 `cdn_url`（ziglang.org/download）加新提取型 `zip-dir`（zip 全量解压不展平，保住 zig-x86_64-windows-0.16.0 版本目录，与 targz-dir/tarxz-dir 对称）。资产下载进缓存后 `pin --version` + 幂等 install 回填 sha256（go=F0C0A0D3...、zig=68659EB5...）。终态 status 28/31 完美接管。
- **feat/遥测关闭（用户指令）**：pwsh——ome msi 分支本就传 `DISABLE_TELEMETRY=1`（与 set-pwsh.ps1 对齐），补齐运行时双保险 `POWERSHELL_TELEMETRY_OPTOUT=1` 与 `POWERSHELL_UPDATECHECK=Off`（对齐 set-pwsh.ps1 L124-126）；dotnet——绿色安装无安装期开关，设 `DOTNET_CLI_TELEMETRY_OPTOUT=1`。新能力 `platform::set_user_env_var`（Windows HKCU\Environment；Linux/mac profile 的 ome env 标记块，`merge_env_exports` 纯函数幂等 upsert），`install_tool` 两条路径（安装与幂等跳过）挂 `ensure_user_env_overrides` 钩子，老环境 install 即补齐。真机注册表三值实证写入（新终端生效）。

## 远端域批次

> 对应 ohmypwsh P0026 M2 联动。

- **feat/all 循环容错**：install/update 的 all 循环单工具失败（未 pin、平台不支持、下载失败等）不再中断整条循环——WARN 后跳过续跑，末尾汇总报错 exit 非零；单工具显式调用仍即时失败。ohmypwsh heal 的 localbin16/toolRoot 键（install all）依赖此语义。
- **fix/平台严格语义**（WSL install all 实证三坑）：其一，go/zig 在 Linux 解析到通用 cdn_url 的 Windows 包（下载了 windows-amd64.zip）——cdn_url() 改平台严格（Linux 只取 linux_cdn_url，mac 取 mac 回退 linux，不回退通用），go/zig 补 linux_cdn_url；其二，git/aria2/7z 等 Windows-only 工具被 effective exe 回退误判在管、走到 resolve 才报错——platform_managed 非 Windows 要求平台专属 exe（linux_exe/mac_exe）；其三，resolve github 分支用了原始 repo 字段——仅 linux_repo 的工具（shellcheck）解析报「缺少 repo」，改 effective repo()。
- **fix/shellcheck 探测三连修**：repo（同上）；正则（真实输出两行，版本在次行「version: 0.11.0」——parse_version 改逐行扫描首个命中，原先只看首个非空行）；tag_prefix 补 v（否则版本解析成 v0.11.0 对不上实测 0.11.0）。中途踩坑两次：match 臂与测试用例重复编译不过/断言挂，均在下一提交修正——教训：改共享表（正则/测试对）先全文搜重。
- **实证/远端幂等接管**：WSL 侧 clone 构建自部署（Linux 71 测试全绿），`ome install all` 终态 WSL 31 项、lan-linux 24 项全 skipped 零 WARN 零 error（lan-linux 二进制与 catalog 由 ohmypwsh ohmywsl.ps1 的 Sync-OmeToRemote 下发，glibc 兼容实证）。遗留登记 TODO：WSL 侧 path=true 但 installed=- 的探测缺口（nushell/bun/starship/fnm/shellcheck 类，待查 linux_exe 探测路径）。

## verify 部署域命令

> 对应 ohmypwsh P0026 M3 第一批。

- **feat/ome verify**：数据驱动维度注册表（静态表按平台生效），判定三层——catalog 工具组三态（locked==installed，强于 ps1 的存在性断言）、文件全在组、任一存在组（aria2 系统位）。Windows 9 维（toolRoot/bun/dev-go/dev-zig/dev-fnm/dev-rust/vsbuild/aria2/rmux），POSIX 7 维（toolRoot/bun/go/zig/localbin16/rmux/aria2）；`--check` 过滤、`--json` 汇总（total/failCount/fails/results）、FAIL 即 exit 1；输出 `dim=PASS/FAIL/NA` 收割行与 verify-five-ends 同构（NA 不参与收割）。密钥、镜像源、残留、secret-guard hooks、mesh、compileMatrix 等非部署域维度仍归 ohmypwsh（边界裁决）。
- **实证/三端全绿**：Windows 9 项、WSL 7 项、lan-linux 7 项全 PASS exit 0，与 ps1 四端验收（70 项全绿）的部署域子集双跑一致——M3 第一批对账达成。
- **顺带关闭**：「WSL 探测缺口」TODO 经平台严格化（platform_exe 双语义）后自解——最新 ome 在 WSL 的 bun/nushell/starship/shellcheck/fnm/go/zig 全部 locked==installed（早前 installed=- 是旧版 exe_path 走 Windows 风格通用 exe 所致）。
- 待办：ohmypwsh verify-five-ends 的部署域维度行接线透传 ome verify（M3 第二批，双跑对账已先行）；compileMatrix 与密钥域维度按边界裁决长留 ohmypwsh。

## 环境

- rustc 1.97.1 / windows msvc；基线 9d86f26 加本日 Windows 提交
