# 2026-08-31-ome立项-文档体系与结构平移

> 一天一篇：当天全部工作的总结与自省。工作细节在方案与研究文档，本篇只留概貌、裁决与得失。

## 今天做了什么

1. 立项：ome（Oh My Env）自 ohmypwsh `ohmyenv.ps1` 剥离为独立 Rust CLI，仓库独立维护在 `D:\ohmyenv\ome`，结构对齐 ohmyagents。
2. 脚手架：Cargo.toml、.gitignore、docs 六类目录骨架、git init。
3. 三原语与数据模式：GOAL / PLAN / TODO / INDEX 建档；R001 落定 `catalog\tools.toml` 字段契约与 pin 回写语义。
4. catalog 转换：`.tools\import-catalog.ps1` 从 ohmypwsh `catalog.psd1` 与 `helpers.ps1` New-ToolDef 静态元数据生成 29 工具名录（进行中收尾）。
5. 文档体系平移：AGENTS.md 四段式按 ome 定位重写；G001-G003、template、R004、R005、R008、R009 自 ohmyagents 平移适配；`.tools` 三个 md 脚本与 `.rumdl.toml` 落位；mistakes 分类位在 INDEX 预留；验证三件套全绿。

## 关键裁决

1. **文档编号沿用 ohmyagents 段位**：G001-G003、R004/R005/R008/R009 原号平移，与已有 R001 不冲突；R002/R003、R006/R007 属 ohmyagents 产品特定（命令细则、rmux、agent 信任），不平移，号段留空不复用。
2. **R004 的 rmux 闸门换写为真机对齐闸门**：依赖真实 `D:\ohmyenv`、用户 PATH 注册表、外网下载的测试由 `OME_TEST_REAL` 环境变量门控，未设则 skip 不失败。
3. **mistakes 先留分类位不建文件**：尚无踩坑记录，INDEX 预留 M101-M105 五个 ome 语境分类（解析下载、解压安装、PATH 注册表、文档命名、工具链脚本），首踩再接编。
4. **平移只改项目名、路径与产品特定例子**：规范本体不动；ohmyagents 专属例证（poc-endpoint、rmux-sdk、oma 命令对照列、S011/S013/S028 研究引用）改写为 ome 语境或注明出处。

## 得失

- 得：文档体系一天内成体系落地，三件套验证可重复跑；AGENTS 意图路由直接挂八命令，后续开发有据可依。
- 失：三原语中的命令行为尚是设计口径（src 仅占位 lib.rs），文档先于实现，需在核心链路开工后逐项用实证回填。
- 自省：平移类工作最易漏改产品特定例子，本轮靠逐文件比对源文兜底；后续大改文档结构后必跑 `md-ref-scan.py` 回归（已写入 AGENTS 与 .tools README 门禁）。

## 收盘追记

1. 核心链路：catalog/resolve/download/checksum 四模块 + query/pin；pin 回写用 toml_edit 保序保注释保 CRLF（踩坑：首轮序列化翻 LF 全文件 diff，已修并留回归测试）。
2. 安装链路：toolver/extract/envpath/install 四模块 + install/deploy/update；extract 九分派全移植（含 7z-extra 引导、gsudo 只取 x64、bunx shim、msi 3010、rmux 官方脚本）。
3. 收尾命令：status 三态分组、daily（同主版本自动、跨主版本保留、exit 2）、self-deploy（复制到 bin + 注册 PATH，sha 比对幂等，实测二次全跳过）。
4. incurs 研究（S001）：不迁移，吸收 OmeError 四元组、render 单一渲染层、帮助元数据化三模式；stdout 只走数据、stderr 走人称提示成文明纪律。
5. 数据保真：转换器合并规则对齐 Get-EnvLock（New-ToolDef 覆盖静态字段），rmux official 值生效；grok 的 New-ToolDef 滞后点记入 M001，后续升级 grok 须先在 ohmypwsh 侧刷新。
6. 真机验收：cargo test 60 全绿；OME_TEST_REAL 闸门 status 29/29、query jq 同 tag；daily 干跑与 pwsh 逐项一致（预览 7/保留 0/已最新 22，退出码同 0）；ohmypwsh 零改动；推送 origin（github.com/raystyle/ohmyenv）。首版目标收盘。

## 接管准备追记

1. 新目标登记：Linux 下接管 ome 开发；GOAL/PLAN/TODO 换锚，历史归档首版目标。
2. R010 落定接管细则：仓库与工具链准备、首次构建验证标准、平台门控现状（envpath/install 已有 cfg 对、winreg 待收 target 专属依赖）、两端验证分工（Linux 跑非 Windows 逻辑、Windows 跑 OME_TEST_REAL 闸门）。
3. README 补「在 Linux 上开发」入口；INDEX 登记 R010；CHANGELOG 补里程碑。
4. 源码核查发现：winreg 在 Cargo.toml 是无条件依赖，Linux 首 build 若暴露即收编，已列入 TODO 门控补齐项。
