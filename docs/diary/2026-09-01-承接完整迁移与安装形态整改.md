# 2026-09-01 承接完整迁移与安装形态整改

> 当天做了什么（项目日记，按日期归档）。

## 工作记录

- **docs/立项**：用户裁决 ohmypwsh 部署、验收、自愈全家桶完整迁移给 ome，取代 R012 共存对齐；仓库改名 raystyle/ohmyenv-rs、本地迁 `D:\ohmyenv-rs`。R012 头部降级标注（数据迁移参考），三原语切到「承接完整迁移」，INDEX/README 边界同步。完整路线在 ohmypwsh 仓库的 P0026 方案（部署迁移 ohmyenv-rs 差集盘点与分域路线）。
- **fix/基线**：新 clone（autocrlf=true 检出 CRLF）暴露测试 bug——`catalog::tests::pin_回写保持原文件crlf行尾` 对夹具 `replace('\n', "\r\n")` 产出 `\r\r\n` 致 TOML 解析失败；改为先归一 LF 再转 CRLF（同 `write_pin` 惯用法）。教训：`include_str!` 夹具内容依赖检出换行形态，凡做行尾转换先归一（实证）。
- **feat/安装形态整改**：按用户指示命令进用户目录、独立 app 数据目录——`platform.rs` 自部署目标 Windows 改 `%LOCALAPPDATA%\Programs\ome\ome.exe`、`data_dir()` 跨平台化（`dirs::data_local_dir()`，注意 5.x API 名不是 local_data_dir）、`metadata_dir()` 三平台统一 `data_local_dir()/ohmyenv`；`selfdeploy.rs` 复制 exe 后同步 catalog 到数据目录并幂等清理旧 PATH 残留 `<EnvRoot>\ome\bin`；`catalog.rs` 解析扩四级：OME_CATALOG、exe 上级、cwd、用户数据目录（cwd 先于数据目录，防部署副本遮蔽仓库开发与测试）。两处小坑：`dirs` 5.x API 名；`mod tests` 裸调父模块函数需 `use super::*`（原非 Windows 测试一直缺，靠 cfg 门控未炸）。
- **chore/清理**：旧 clone `D:\ohmyenv\ome` 同时是旧自部署位与 catalog 来源，先新位部署验证（`ome status` 任意目录可跑、走数据目录 catalog）后删除。

## 关键结论

- `catalog\tools.toml` 的 `linux_*` 字段在 working tree 与 git 历史均为 0：2026-09-01 重跑 import-catalog.ps1 加 reader 时托管节被再生、Linux 字段整体丢失；psd1 Pos 侧是唯一现存数据源（实证）。M0 回流前禁改 psd1 Pos 侧。
- `effective_*` 全为非 Windows 分支读 `linux_*`，mac 复用同组字段；mac 字段族缺失是 M1 结构性前置（实证）。
- 68 测试全绿、fmt/clippy 零告警、两个 md 扫描零问题（实证，本机 rustc 1.97.1）；gnu 交叉 check 本机不可跑（无 target 加 C 工具链前提，环境前提未满足）。

## 环境

- rustc 1.97.1 / windows msvc；基线 993e77b 加本日提交
