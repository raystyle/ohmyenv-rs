# 2026-09-10 typst 入册与 v0.15.1 锚核验

## 起因与范围

- 用户指令「增加 typst v0.15.1 的安装」，点名 release tag（`github.com/typst/typst/releases/tag/v0.15.1`）。
- 范围：catalog 入册（三平台静态字段加 pin 四键）、计数同步、本机真机安装验收；不改 CLI 命令面（D10 三原语口径不动）。

## 锚核验

- 上游 release 资产列表无 SHA256SUMS / checksums（无统一校验清单），锚形态取 GitHub digest（lightpanda D24 先例）。
- 三平台资产本机下载实测 sha256 与 digest 逐字一致：win `typst-x86_64-pc-windows-msvc.zip` 19CE3551、linux `typst-x86_64-unknown-linux-musl.tar.xz` A6D077D0、mac `typst-aarch64-apple-darwin.tar.xz` 48F62ED0。
- 多架构同名族防错配（M014 同型）：`aarch64-pc-windows-msvc`、`x86_64-apple-darwin`、`riscv64gc`、`armv7` 等旁支资产逐个对照名字与 digest 行，不按命名惯例推断。

## catalog 节与布局

- 归 cli 类（命令工具依赖），节尾入册；探测走 `typst --version`（实测输出 `typst 0.15.1 (9dfd3a08)`），正则 `typst (\d+\.\d+\.\d+)`。
- win zip 内含 `typst-x86_64-pc-windows-msvc/` 单包裹层，`extract = "zip"` 展平后 typst.exe 居 dir 根；linux / mac 的 tar.xz 各为单二进制，`tarxz-bin` 按叶子名取 typst 落 `~/.local/bin`。
- mac 取 aarch64 资产（ome mac 为 ARM）；上游另有 x86_64-apple-darwin，不设 Intel 字段。

## 真机验收

- `ome query typst`：tag v0.15.1、资产与 sha 与锚一致（size 22463684 与官方一致）。
- `ome install typst`：首装 installed（`D:\ohmyenv\typst\typst.exe`）加 PATH 注册；幂等二连 skipped 零副作用。
- `ome status` typst 行三态齐：locked=installed=0.15.1、path=true。
- `seed.py --plan`：101 对象 97 synced，typst 三件 sidecar-missing（待 catalog 推送触发 seed-mirror 路线 B 自动入镜）；无 sha-drift 与既有对象错配。

## 门禁与文档同步

- cargo test 全绿（lib 90 加 cli 28 加 catalog_lint 2 加 golden 2 加 install 2 加 mirror 7 加 real 7）；黄金文件未破（夹具 catalog 不驻 typst，status 与 pin oracle 无涉）。
- `rumdl check .` 与 `.tools` 三扫描全绿；计数 46 改 47（AGENTS 两处、README 三处含类表、SKILL、INDEX、catalog 头注释）。
- G001 检查顺修：INDEX 日记表补 2026-09-08 与 2026-09-09 两行历史欠账（磁盘对账发现，非本次产生）。

## 观察与候选

- `cargo clippy` 一条存量告警（`src/catalog.rs:448` doc 注释第二行 `>` 起首成 markdown 引用，第三行续接触发 doc_lazy_continuation）：D31 提交带入，与本任务无关，CI 只跑 cargo test 故未暴露；随手小修单独成笔。
- `seed.py --plan` 会在工作树写 `catalog/tools.toml.sha256`（上传用边车直接落归档区旁，非产物入仓项）：本次移至 Temp 未入库，候选为 .gitignore 一行或改临时目录写入，待裁。
