# 2026-09-12 D41 立项更名 Ark 迁移计划

## 起因

- 用户 2026-09-11 定夺 ome 正式更名 Ark（Agent Runtime Kit）；2026-09-12 补裁：GitHub 仓已改 raystyle/ark-rs、本地目录将迁 `D:\ark-rs`、首发版 1.0.0。催办两件：工作态收净后回「可迁」；迁移计划先落、herdr 对线后再动码。
- 用户顺告 oma-state.cmd hook 刷屏已修：根因是本机 `~/.oma` 被清理轮误删（settings 托管 hook 在但 shim 缺失），`oma init --pretrust` 重建后 doctor 绿。该形态已 herdr 转递 oma 会话，建议纳入 hst 迁移 heal hooks 覆盖面（heal 判据需探 shim 实体，不能只校验 settings 面）。

## 落档

- PRD D41 登记七点口径（已采纳）；GOAL 锚点切 D41 加时间线一行；PLAN 换档为四阶段（A 身份核心、B 分发链、C 自举与存量兼容、D 文档与发版）与七面自测；TODO 开 D41 行；INDEX 日记表挂链。
- 时序定死：目录迁移与 herdr 计划对线先于任何代码改动；云端 ark/ 段与 catalog 自管条目更名走 herdr 协调 omc，切换期双读不破供给。

## 下一步

- 用户 mv 加 remote set-url 后于 `D:\ark-rs` 重开会话；先 codex 对线本计划，回执入 diary；随后按 A 至 D 落码，收口 v1.0.0 封版（三平台 CI 绿加 ark/stable 直推加 self update 部署位验收）。

## codex 对线三轮收敛（CONFIRM）

- 迁移完成（`D:\ark-rs` 就位、remote 已切、会话历史完整恢复）后按 PLAN 第一动作起 codex（wB:p9；起手两坑：新目录信任弹窗、hooks 信任弹窗，应答后正常）。评审方法：仓内身份面 grep 加两次只读网络探测（`env.ohmygh.com` 现役段 200、GitHub API 301 重定向 ureq 跟随、旧 release 资产名三名），实证进结论。
- 首轮 16 条全采纳。阻断 8：R1 自产镜像段须**双写**（旧二进制回落硬编码 ome/ 段，只读兼容会断存量机升级链）；R2 v1.0.0 release 须双附 `ome-*` 兼容资产名（301 救不了资产名缺失）；R3 omc 灌 ark/ 须**续 seq 序列**不重计数（否则 D40 门拒收）；R4 搬迁清单漏 manifest 三件套（七件套：tools.toml、两 minisig、manifest.toml、两 .seq、.last-sync）；R5 目录搬迁即回滚门重置（seen=0 窗口）；R6 ome 别名载体未定且与 PATH 清理自相矛盾（定：部署位同目录 exe 副本，init 与 self update 重建）；R7 profile 写入面漏三处（env 块、fnm 钩子、ohmyenv-secrets 载体）；R8 `ome-self` extract 六消费分支遗漏。建议 8：doctor 三探针 URL（R9）、git 通道产物名按 `CARGO_PKG_NAME` 派生（R10）、签名密钥位裁定（R11，裁：保留 `~/.config/ome/` 不迁移）、Windows 旧部署位清单（R12）、crate 与测试 harness 原子改名（R13）、文档残留 grep 门（R14）、仓内 agent hook 指旧目录（R15）、内部临时名与 UA（R16）。自测七缺口同步吸收（跨版本真机升级、双变量优先级、段三态矩阵、别名可调三平台、探针同源机检等）。
- 复核轮五条残差收口：双写须同一 job 对两段各跑 seed.py；seq 补记改「首刷 + 并存期取 max + 退役后冻结」；rg 残留门恢复词边界（`\bome\b`，我引入的缺陷，防 home/some 误报）；omc 知会补命令面契约（agent deploy 调用改 ark install 加别名窗口）；对线口径统一两轮。末轮两条 R5 余留：自测 1 改两断言；旧目录冻结判据与停段同源挂存量机水位清零。终 CONFIRM。
- 教训候选：计划评审也要网络实证（现役段 200 与 301 行为改变结论层级）；「读兼容」与「双写供给」是两件事，凡有存量消费方的面，切换期先保供后切名。
