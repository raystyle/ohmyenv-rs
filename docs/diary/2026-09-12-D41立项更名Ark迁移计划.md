# 2026-09-12 D41 立项更名 Ark 迁移计划

## 起因

- 用户 2026-09-11 定夺 ome 正式更名 Ark（Agent Runtime Kit）；2026-09-12 补裁：GitHub 仓已改 raystyle/ark-rs、本地目录将迁 `D:\ark-rs`、首发版 1.0.0。催办两件：工作态收净后回「可迁」；迁移计划先落、herdr 对线后再动码。
- 用户顺告 oma-state.cmd hook 刷屏已修：根因是本机 `~/.oma` 被清理轮误删（settings 托管 hook 在但 shim 缺失），`oma init --pretrust` 重建后 doctor 绿。该形态已 herdr 转递 oma 会话，建议纳入 hst 迁移 heal hooks 覆盖面（heal 判据需探 shim 实体，不能只校验 settings 面）。

## 落档

- PRD D41 登记七点口径（已采纳）；GOAL 锚点切 D41 加时间线一行；PLAN 换档为四阶段（A 身份核心、B 分发链、C 自举与存量兼容、D 文档与发版）与七面自测；TODO 开 D41 行；INDEX 日记表挂链。
- 时序定死：目录迁移与 herdr 计划对线先于任何代码改动；云端 ark/ 段与 catalog 自管条目更名走 herdr 协调 omc，切换期双读不破供给。

## 下一步

- 用户 mv 加 remote set-url 后于 `D:\ark-rs` 重开会话；先 codex 对线本计划，回执入 diary；随后按 A 至 D 落码，收口 v1.0.0 封版（三平台 CI 绿加 ark/stable 直推加 self update 部署位验收）。

## codex 对线三轮收敛并 CONFIRM

- 迁移完成（`D:\ark-rs` 就位、remote 已切、会话历史完整恢复）后按 PLAN 第一动作起 codex（wB:p9；起手两坑：新目录信任弹窗、hooks 信任弹窗，应答后正常）。评审方法：仓内身份面 grep 加两次只读网络探测（`env.ohmygh.com` 现役段 200、GitHub API 301 重定向 ureq 跟随、旧 release 资产名三名），实证进结论。
- 首轮 16 条全采纳。阻断 8：R1 自产镜像段须**双写**（旧二进制回落硬编码 ome/ 段，只读兼容会断存量机升级链）；R2 v1.0.0 release 须双附 `ome-*` 兼容资产名（301 救不了资产名缺失）；R3 omc 灌 ark/ 须**续 seq 序列**不重计数（否则 D40 门拒收）；R4 搬迁清单漏 manifest 三件套（七件套：tools.toml、两 minisig、manifest.toml、两 .seq、.last-sync）；R5 目录搬迁即回滚门重置（seen=0 窗口）；R6 ome 别名载体未定且与 PATH 清理自相矛盾（定：部署位同目录 exe 副本，init 与 self update 重建）；R7 profile 写入面漏三处（env 块、fnm 钩子、ohmyenv-secrets 载体）；R8 `ome-self` extract 六消费分支遗漏。建议 8：doctor 三探针 URL（R9）、git 通道产物名按 `CARGO_PKG_NAME` 派生（R10）、签名密钥位裁定（R11，裁：保留 `~/.config/ome/` 不迁移）、Windows 旧部署位清单（R12）、crate 与测试 harness 原子改名（R13）、文档残留 grep 门（R14）、仓内 agent hook 指旧目录（R15）、内部临时名与 UA（R16）。自测七缺口同步吸收（跨版本真机升级、双变量优先级、段三态矩阵、别名可调三平台、探针同源机检等）。
- 复核轮五条残差收口：双写须同一 job 对两段各跑 seed.py；seq 补记改「首刷 + 并存期取 max + 退役后冻结」；rg 残留门恢复词边界（`\bome\b`，我引入的缺陷，防 home/some 误报）；omc 知会补命令面契约（agent deploy 调用改 ark install 加别名窗口）；对线口径统一两轮。末轮两条 R5 余留：自测 1 改两断言；旧目录冻结判据与停段同源挂存量机水位清零。终 CONFIRM。
- 教训候选：计划评审也要网络实证（现役段 200 与 301 行为改变结论层级）；「读兼容」与「双写供给」是两件事，凡有存量消费方的面，切换期先保供后切名。

## A 阶段落码与对线 CONFIRM

- A1 `914c4b2` crate 与 CLI 切 ark 1.0.0：Cargo/clap/LLMS/示例与用户面文案/use ome:: 全量；测试 harness `cargo_bin` 与 `CARGO_BIN_EXE` 同批。踩坑一次：批改脚本误改 `SELFTEST_SIG` 注释行，而 trusted comment 属被签内容，改字即破签（红测抓回，逐字回滚）；该常量注释本有「逐字保留」警告，脚本不吃警告。
- A2 `de0bbf1` 环境变量族：`platform::env_var_or`（主名优先、空白视同未设、旧名读回）；七消费点切（ARK_ROOT/ARK_CATALOG/ARK_CATALOG_TTL 与 ARK_OFFLINE/ARK_MIRROR/ARK_TEST_NO_PATH_REG）；测试闸门双读、注入切主名；新增优先级单测与旧名 CLI 回退断言。
- A3 `0842abb` 元数据目录与部署位：`resolve_metadata_dir` 纯函数三态（皆无主名、旧 ohmyenv 在读回、新 ark 在归位）；`self_deploy_target` 切 `Programs\ark\ark.exe` 与 `~/.local/bin/ark`（旧位处置归 C）。
- A4 `13f559a` 加 `89625af`：`is_ome_self` 双接受 ome-self 与 ark-self（六消费点单点收口）；git 通道产物名按 `CARGO_PKG_NAME` 派生；UA 与临时名清扫；seed 双脚本 evergreen 集合加 ark-self；clippy 告警清零。
- 对线（用户点名 herdr 走 codex）：评审六结论：1 阻断（build.yml `matrix.bin` 未随 crate 改，A 单推 CI 归位步必挂且连坐 mirror-r2 断 dev 段供）加 4 建议（错误前缀 `ome:`、render_skill 标题半更名、doctor `is_derived_asset` 只认 ome- 前缀会在 B 双附后误报孤儿、三处漏测）加 1 低（D 段残留留批量裁定）；越界面 CONFIRM（B/C 面的镜像段、云端键、profile 标记块、公钥、REPO 全部原样未动）、替换损伤零。修复笔 `ed240fc`（bin 切 ark 而 asset 名留 B 双附：bin 随 crate、asset 保兼容，两层解耦）加 nit 收尾 `c8bf719`（ark- 前缀断言）；复核 CONFIRM。lib 110 加 cli 30 全绿、clippy -D warnings 清零。
- 待推提示：A 批八笔在本地未推（未经指示不推）；bin 修复后 A 单推 CI 不再断。

## A 批推送与 CI 三平台绿

- 用户裁「先推 A 批看 CI」：12 笔推达 `6e5f28a..68451e4`；run `34690001163` 全绿（build 三平台 matrix 名见 `ark`/`ark.exe` 产物加 `ome-*` 兼容资产名、mirror-r2 续灌 ome/dev 段）；镜像三资产边车 HEAD 200（新 ark 1.0.0 二进制以 ome-* 名续供，存量机不断供）。A 落定。

## B 分发链落码与对线

- B1 `0c75ced`：selfupdate REPO 切 raystyle/ark-rs；资产名单源派生（三元组出主名 ark-* 与兼容名 ome-*）；镜像段读序 ark/ 先 ome/ 回落（`mirror_attempts` 纯函数加矩阵单测）；官方 release 主名缺试兼容名并如实上报命中名；doctor 三探针由 REPO 与 `mirror_url` 派生加同源机检单测。
- B2 同笔：build.yml 资产双附（matrix 加 compat，双 cp 双 `gh release upload`）；mirror-r2 双写（同一 job 对 ark 与 ome 两族各跑一次 seed.py，`&&` 串接任一失败即失败）；seed.py 参数化（`--ark-dev`/`--ark-stable` 主名旗标、`--ome-*` 兼容写、repo 新名、对账源加 ark 主名候选）。
- 对线两轮：首轮 1 阻断（官方成功分支的镜像段内回落硬编码 ark 段；过渡窗 release 仅 ome-* 时官方命中的是兼容名，回落应配 ome/ 段，否则 ark/ome-* 拼出 404 断保供。修法 `fallback_seg` 纯函数按命中资产族取段，三态单测）加 2 建议（seed-inventory 报告正文 ome/latest 双重过期行收口、.tools/README 清单行补双写用法）加 1 低（B 回执本笔补记）；复核轮 nit 收尾（`fallback_seg` 抽纯函数）。codex 真网实证在案：ark/ 段当窗 404、ome/ 段 200、release 仅 ome-*，读序回落推演成立。
- **R3 缓办判据**：ark/ 段命中态 gated 用例待首轮 B CI 灌段后补（`ark/dev` 边车 200 即可加「ark 命中」腿；先行即红故缓）。
- B 批两笔在本地未推；推送后首轮 CI 即产出 ark/dev 段（双写落地实证点）。

## B 推送与双写双附实证

- 用户裁「推 B」：五笔推达 `68451e4..a143527`；run `34691438860` 全绿（三平台 build 加 mirror-r2 灌段双写 step）。**双写双附实证**：ark/dev 段三资产边车 200（首轮 ark/ 段落地）、ome/dev 兼容段 200 续供、release dev 六资产（ark-\* 三加 ome-\* 三）。R3 缓办前置满足。

## C 自举与存量兼容落码与对线

- C1 `7060549`：云端键切 `ark/catalog/` 主名、双键读序回落（边车探活、清单拉取、manifest 三件、status 全链；URL 构造键参数化）；R3 缓办收口（ark/dev 主段 gated 用例加双段同内容断言，真网 7 过）。
- C2 `7bf73b1`：元数据七件套搬迁（`METADATA_MIGRATION_FILES`：tools 与 manifest 本体加双 minisig 加双 .seq 加 .last-sync；幂等 copy 旧位只读保留；init 的 deploy_catalog 与 self update 双通道接线）；seq 地板 `seen_seq_floor` 新旧目录水位取 max（三门全换；冻结判据与停段同源）。
- C3 `d9e3dd7`：profile 全写入面双读迁移（PATH 块 ark 写 ome 读序合并退役、env 块跨块同 KEY 合并替换、fnm 钩子标记幂等改写）；**裁定**：heal 密钥载体 `~/.config/ohmyenv-secrets` 保留旧名（数据位非元数据、与 EnvRoot 物理目录同类零迁移，rc 行幂等改写无收益）。注：当日 Windows 侧 lib 计 116（PATH 块 POSIX 用例不计入本机），跨平台合计 117。
- C4 `cf65017`：init 接管旧部署位（Windows `Programs\ome` PATH 条目加目录 best-effort 收口，运行中锁目录留待下次）；`ome` 别名 = 部署位同目录 exe 副本（init 与 self update 重建；POSIX 旧位 `~/.local/bin/ome` 即别名落点自然接管）。
- 对线（codex）：**零阻断**，五大面 CONFIRM（键锚同段成对、搬迁与地板、profile 迁移、接管与别名、D 面零越界）；四建议全收：①过渡窗主键 404 走完整重试链拖慢命令（实测 `catalog status` 9.5s），改单次短探定键再走完整链；②PATH 块退役懒触发，`add_user_path` 检测旧标记即无条件落写一次；③本笔回执与原语同步；④键读序数组加机检断言。修复后 lib 118 加全套绿加 clippy 清零。
- C 批五笔在本地未推（`7060549..` 修复笔）；推送即触发三平台 CI 对 POSIX 面（PATH 块与 hook 用例）首轮编译验证（M016 口径）。

## C 推送与 D 文档发版

- C 推达 CI 全绿（run `34693424425`：三平台 build 加 mirror-r2，POSIX 面首轮编译与用例过）。
- D 文档批 `49fd56e`：README 与 SKILL 全量重写 ark 身份；AGENTS 定位边界路由切 ark（三仓路径 `D:\ark-rs`）；CHANGELOG 封 **1.0.0**（更名 major、兼容红线、Unreleased D40 项并入）；ROADMAP 阶段八（更名 Ark 与 v1.0.0）；R001 泊位术语定档（berth = EnvRoot 内安装位，EnvRoot 为泊位根物理目录不动）；R013 冻结面登记 D41 breaking 与兼容层三面（别名、环境变量读回、双写双附）加 omc 契约面换 `ark install` 过渡窗；R015 密钥位保留裁定入表；R004 闸门主名；R014 委托口径；R016 活语义节；R010/R011/R012 加更名注；INDEX 全同步；tests helper `run_ome` 改 `run_ark`。
- **残留门裁定（rg -i "ohmyenv|OME_|\bome\b" 非归档区逐条裁定）**：正文保留面限三类：①兼容口径（旧名读回说明、别名、双段双键）；②历史记录（GOAL/TODO/PRD 时间线行、diary、dated 对线与 [实证] 条目、issue 旧号 `ohmyenv-rs#10` 沿用注）；③数据面在册名（自管条目现名 `ome`，更名归 omc）。研究区（docs/research）与 mistakes 全区豁免（历史档）。活语义文档（README/SKILL/AGENTS/INDEX/R001/R004/R013/R014/R015/R016/CHANGELOG/ROADMAP/PLAN）已切。
- 发版序列（用户裁 2026-09-12）：文档批推 main 过 CI 后 herdr 知会 omc（ark/ 铺段续 seq、双写窗口、停段判据、自管条目更名、命令面契约 ark install 加别名窗）与 oma（命令面依赖确认），tag `v1.0.0` 推送走 CI v* 通道（正式 release 双资产名加双 stable 段直推），三平台绿后 `self update --stable` 部署位验收；停 ome/ 面（段与资产名）判据为存量机水位清零，届时 omc 联动批切。

## v1.0.0 发版与部署位验收收官

- 文档批推 main CI 绿（run `34694087809`）；tag `v1.0.0` 推送，CI v* 通道全绿（run `34694344089`：三平台 build 加 mirror-r2 双 stable 段直推）。正式 release 六资产双名同 digest 逐对一致（win 27B0B9F3 加 linux 9F098F00 加 mac FEFDABEA，ark-\* 与 ome-\* 各自成对相等）；ark/stable 与 ome/stable 双段边车 HEAD 200（首轮 stable 主段落地）。
- herdr 知会双发：omc 五点（铺段续 seq、停键判据、自管条目加新 tools.ark 策略、deploy 契约 ark install、锚同步）与 oma 依赖确认；omc 回执已至：**catalog-seed 双发已落地实证（ark/catalog/ 与 ome/catalog/ 同内容同 seq=9 续号无重计）**，等资产 sha 知会后做三件批切（catalog 节、deploy 命令串、tool 域），sha 三平台已 herdr 送抵。
- 部署位验收（本机）：`self update --stable` **抓到真 bug**：`replace_exe` Windows 分支在部署位首次落位（无前代文件）时 rename 缺文件即败（v1.0.0 已发版故落 main 走 dev 通道修复：首次落位直接写新件，加三段回归单测；`.old` 保留属设计，断言按设计修正）。修复后验收全绿：`action=updated` 至 `Programs\ark\ark.exe`、`ome` 别名同位可调（两者 `ark 1.0.0`）、元数据七件套搬迁至 `AppData\Local\ark`（旧 ohmyenv 只读保留）、`catalog status` origin=userdata 加 synced=true 加 signature=valid 加 manifest_synced=true（**seq 10 通过**，omc 批切进行中的实证）；`ark init` 接管收口：旧 `Programs\ome` PATH 条目移除加目录删除、PATH registered。
- D41 四阶段（A 身份核心、B 分发链、C 自举与存量兼容、D 文档发版）全部收官；存量面（旧二进制经 ome-\* 兼容资产自更新、omc 三件批切、停 ome/ 面水位观测）转入过渡期运营。
