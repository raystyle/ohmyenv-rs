# 2026-09-01：新增 reader 工具与构建修复

## 今天做了什么

1. 新增工具 reader（[实证] raystyle/reader_rs v0.1.0，Windows 资产 reader-v0.1.0-x86_64-pc-windows-msvc.zip + .sha256 旁车）进 `catalog\tools.toml`（27 个工具，托管 26 + 本地 1）。
2. 转换器 `.tools\import-catalog.ps1` 增加本地节保留：非 ohmypwsh 托管的 `[tools.x]` 节重跑时原样保留在尾部（[实证] 重跑字节级幂等）。
3. `toolver.rs` 加 reader 版本正则与真实输出样例测试（`reader 0.1.0`）。
4. 修复并行会话（Linux/mac 支持）留下的 Windows 构建问题（[实证] 本轮 cargo test 复现并修复）：
   - `extract.rs`：PathBuf 导入被 cfg(not(windows)) 门控但测试模块跨平台使用，改 cfg(any(not(windows), test))。
   - `platform.rs`：三个 Linux 语义测试（expand_env_vars/is_official_exe/path_entries_eq）在 Windows 必败，补 cfg(not(windows)) 门控。
   - `install.rs` / `status.rs`：expand_install_path 对 Windows 相对 dir/bin 不再拼 EnvRoot，导致防穿越误拒与 PATH 误判；补 join_if_relative。
5. 真机验证：`ome deploy reader` 安装到 `D:\ohmyenv\reader` 并注册 PATH，status 三态 locked=installed=0.1.0、path=true；`reader --version` 输出 0.1.0。
6. `tests\real.rs`：闸门断言更新为 ome 减 pwsh 恰为 reader、pwsh 减 ome 恰为三智能体；OME_TEST_REAL 全绿。

## 关键裁决

1. **reader 走 ome 本地名录**：不改 ohmypwsh（边界规则），转换器从「全量生成」演进为「托管 26 节生成 + 本地节保留」（[实证] import-catalog.ps1 头部注释与实现）。
2. **sha256 用官方旁车实证值**：pin 的 sha256 取自 release 的 .sha256 文件并与本地下载复算一致（[实证] 199362B5…80C680）。

## 得失

- 得：本地新增工具的完整链路（名录、转换器保留、版本探测、真机部署、闸门断言）一次打通，后续新增工具照此办理。
- 失：并行会话的 Linux 侧改动未在 Windows 编译验证就留置工作区，三个构建问题由本轮兜底；后续跨平台改动应在提交前双端跑 cargo test（[推断] 可记入 R011/R004 门禁提醒）。

## AGENTS 对齐自省追记

1. 起因：ohmyagents AGENTS.md 结构演进（新增工作节奏规则 5「经验沉淀时」强规则，G004 细则），ome 对齐。
2. 漂移核查：G001-G003 与 ohmyagents 逐文件 diff——差异均为有意的平移适配（ROADMAP 行、示例列、产品特定例子），无实质漂移；G002 缺 G004 衔接段，已补。
3. 对齐落地：ome AGENTS 插入规则 5（经验沉淀时，引用 G004；R002 换 INDEX），提交时移 6，写作编码 7-12 重编号；G004 全文平移适配；INDEX 登记 G004。

## reader 真实负载与研究追记

1. reader 真机验证：新终端 PATH 生效；对 D 盘《Command-Line Rust》PDF（5.8MB）extract/search 实测可用，前 10 页扫描件无文字层报 needs_ocr，正文自 11 页起正常。
2. S002 研究归档：用 reader 对全书做定向研究（search 定位 + extract 精读 + 页码证据），结论：ome 的错误体系/clap 形态/闸门纪律已在该书水平之上，值得吸收的是测试侧三件套（expected 文件 oracle、dies_ 成组过滤、cfg 双平台期望），与 R004 演进路线衔接；INDEX 已登记。

## S002 测试三件套落地追记

1. expected 文件 oracle：新建 `tests\common\mod.rs`（`run_ome` + `assert_stdout_eq_golden`，CRLF/沙盒路径/分隔符归一化）与 `tests\expected\`（pin.txt、status.txt 两个黄金文件，头部 `##` 注释记来源纪律）；`tests\golden.rs` 两个全量比对测试（pin 无选项、status 沙盒）。
2. dies_ 成组过滤：负例补齐 pin 的 --latest/--version 与 --tag/--version 互斥、pin 未知工具、catalog 缺文件四个；`cargo test dies` 收拢 10 个负例全绿。
3. R004 补「expected 文件 oracle」小节（何时用、目录约定、冻结纪律、helper 用法、归一化纪律、与 OME_TEST_REAL 分工、cfg 双平台期望触发条件），验收自查加一条；INDEX 目录结构登记 tests\expected。
4. 验证：cargo test 66 绿（含 2 个新黄金测试与 4 个新负例），rumdl 与两个 md 扫描全绿。
5. 遗留：本机 toolchain 1.97.1 下 `cargo fmt --check` 与 `clippy -D warnings` 在既有 src 文件（checksum/extract/platform 等）与 tests\linux_install.rs 上报漂移（格式风格与新 lint），均非本轮改动引入，待统一处理。

## AGENTS 对齐自省追记（第二轮）

1. 起因：ohmyagents AGENTS.md 又有三个提交（G004 重写、规则 5 断行修复、serve 停机口径）。
2. 核查结论：G004 重写与规则 5 均发生在 ome 首轮对齐之前，ome 已含；断行修复的拆行 ome 初版即如此；serve 口径属 ohmyagents 产品路由，不适用于 ome。G004 diff 复核仅四处有意适配（平移注记 + R002 换 INDEX 三处）。
3. 处置：零漂移，无代码文档改动；本轮核查留此流水备查。
