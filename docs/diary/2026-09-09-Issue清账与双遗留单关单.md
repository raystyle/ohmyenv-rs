# 2026-09-09 Issue 清账、双遗留单关单与 D28 入册清单化

## #3/#4 过时遗留关单

- 起因：仓库巡检发现两 OPEN 单（2026-09-02 立），ome 侧交付早已完成而单未关。
- #3（M4 heal 移植）：`ome heal [dim|all] [--dry-run]` 与 heal-map 42 键嵌入注册表 09-02 已收口（双平台破坏-自愈-verify 闭环、heal all 两连零 diff）；验收口径里的 ohmypwsh 双跑对账、heal 臂薄转发随 D18 消亡。
- #4（query/status json 契约冻结）：S003 渲染层（kv/json/jsonl）与 query sha256 契约冻结 09-02 已落；`ome package` 能力随 D17 显式删除，镜像构建离线装料场景由 ohmycloud 分发基建承接（D19）。
- 两单均写回执评论后关闭；本仓 issue 队列清零，工作树净。

## D28 入册清单化

- 起因：09-08 diary 候选（toolver 正则漏带两犯 rclone/gitleaks、digest 错配 M014 的统一机检），用户「继续」立项推进。
- 迁移：toolver `version_args`/`version_pattern` 两 match 表删源码迁 catalog 字段 `probe_args`（缺省 `["--version"]`）与 `probe_pattern`（第 1 捕获组）；命名避开已占用的 `version_pattern`（python 资产提版语义）。12 处调用方签名从 `&str` 改 `&Tool`（docker/vsbuild 的 relaunch_elevated 与 ensure_machine_path 补 def 参数透传）。
- 回填：`.tools/inject-probe-d28.py`（仿 D25 注入器，category 行前插）46 节一次到位（8 特例参数：7z --help、rmux/openssh -V、oscdimg 空参、vsbuild -version、zig/go/lightpanda version 子命令）；fixtures 三节同步；catalog 头注释 41 改 46 顺修。
- 机检：`tests/catalog_lint.rs` 真仓与夹具同规则（在管必有 probe_pattern、正则可编译且含捕获组、sha 族 64 hex）；cargo test 常规面红灯拦漏，不再靠装后实测暴露。toolver 单测改构造 Tool，期望值（真实输出样例 oracle）不动。
- 坑：本会话 shell 无 VS 链接环境（LNK1181 legacy_stdio_definitions.lib 找不到；补 LIB 后 lzma-sys 又缺 INCLUDE），经 vcvars64.bat 导 16 环境变量落 Temp 复用解决；install 沙盒幂等测试红：动态 catalog 的 jq 节缺 probe_pattern 致探测 None 幂等短路失效触网，补字段即绿（恰好实证机检价值：任何 catalog 消费面漏字段当场暴露）。
- 验证：cargo test 全绿（lib 91 加 cli 28 加 catalog_lint 2 加 golden 2 加 install 2 加 mirror 7 加 real 7，黄金文件未破）；clippy 干净；真机新构建 status 抽查八工具探测全对（zig 子命令、openssh -V、7z --help 特例全命中，lightpanda win 空态如实）。
- 文档：R001 字段表两行加「入册 checklist」节（五；evergreen 顺移六并同步 INDEX 交叉引用），pin 四键同 tag、seed.py --plan 域面 diff 对账进纪律；CHANGELOG、INDEX、.tools README 同步。

## D28 推送验收与 CI 消音

- 推送 a601aae：CI 双 job 绿（build 含 mirror-r2 route A；seed-mirror 98 对象 synced、uploaded 0、failed 0，probe 字段不动种子域面如预期零差集）。
- 顺手消音：两 workflow 三处 actions/checkout@v4 升 @v5（GitHub 强制 Node 24 运行时的弃用警告），push 即验证。
