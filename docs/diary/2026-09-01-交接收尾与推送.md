# 2026-09-01 交接收尾与推送

> mac 侧三个批次（M1 mac 字段族、M0 数据主权与回流、mac 完美收敛）全部收盘，开发接力回 Windows 做 ohmypwsh 联动验收。

## 做了什么

1. **接力文档刷新**：R011 一.4 从「mac 待办 M0」刷新为收官交接态，写明 Windows 回接要点——基线门禁清单（Windows cfg 测试回归、校验器缺省命中 D:\ohmypwsh）、平台 pin 分列纪律（Windows 回写只动通用四键，mac 真机写入的 sops/uv/vault/go pin 与 sha 不得覆盖）、转换器已改只校验（勿再跑旧版生成）、tag_prefix 未写即无前缀的语义变更、mac 遗留观察（pwsh/go path 态、~/.ohmyenv/bin 残留）。
2. **三原语交接**：TODO 头部新增「Windows 回接与基线门禁」「ohmypwsh 联动验收」两项（P0026 M6 口径），GOAL 时间线与进程登记交接节点，PLAN 增第 7 步。
3. **推送远端**：10 笔提交（M1 两笔 + M0 四笔 + mac 完美收敛三笔 + 本笔）推 origin/main，Windows `git pull` 即得全量。

## 自省

- mac 侧一天三批次收官靠的是「真机全量过一遍」而不是抽样验证——18 工具三态全等是逐工具 install/status 跑出来的，三枚暗礁（version 子命令、无前缀 tag、cdn 不看 pin）全是全量跑到才现形。接力回 Windows 后同样别信「应该没问题」，cfg 回归测试重新全量跑。
- 交接文档的关键不是复述做了什么，而是写清「对面会踩什么」：平台 pin 不可覆盖、校验器语义变更、前缀默认变更——这三条是 Windows 侧最容易按旧习惯踩的。

## 下一步

Windows 侧（用户接管）：回接门禁 → ohmypwsh 联动验收（M6 口径）→ M2 远端下发。mac 侧随时按 R011 复验。
