# 2026-09-07：dotfiles 吸收与双工具入册

> 参考吸收 raystyle/dotfiles（Intel mac 装机仓）：zoxide 与 sheldon 入册（用户两裁），下载链四段模式进 D08 设计，镜像对账一致，zsh/starship/sheldon 配置栈记档学习。

## 做了什么

1. **dotfiles 摸底**：仓概貌（Intel macOS 专用、deploy.sh 模块化、cli/manifest.tsv 的 Agent 友好栈 13 工具）、`lib\download.sh` 四段下载链（gh、api.github.com、aria2c、curl 依次回落，硬超时，镜像前缀，github web 停滞探测）、cargo rsproxy 与 go goproxy 镜像配置。
2. **吸收裁决**（用户）：首轮「暂只吸收模式」，旋即改裁「只加 zoxide，其余吸收学习 zsh starship sheldon 配置方式」，再追加「sheldon 也加」。
3. **zoxide 入册**：首版 pattern 带 v 前缀全不匹配，gh api 实证资产名不带 v（tag v0.10.0、资产 zoxide-0.10.0）后修正；win zip 根级 zoxide.exe 直中（dir=zoxide）；toolver 补正则后 install 幂等加 sha 回填加 deploy 全绿（Windows 本机闭环）。
4. **sheldon 入册**：上游无 Windows 资产也无 darwin-x86_64（0.8.5 实测仅 linux musl 三架构加 aarch64-darwin），Windows 走 platform_managed 空态（shellcheck 先例）；条目初版漏通用 repo 字段被解析报错抓出补上。
5. **WSL 双端验证**：self update 后 catalog 有条目但 pin 键缺（远端 main 未含本地回写），手动同步仓库 catalog 后 `ome pin` 双家解析绿（zoxide v0.10.0 linux musl、sheldon 0.8.5）；install 下载解压双就位（二进制可跑），状态回写被旧 ome 验版本步误拦（旧二进制无新 toolver 正则），新 dev 资产后自愈。
6. **S004 落档**：吸收裁决表、下载链四段模式（D08 输入）、配置栈学习、镜像对账（cargo rsproxy 与 go goproxy 均与 ome 现状一致）、intel mac 三应对（pin 末版、allow-compile、跳过）；INDEX/AGENTS/README 同步 43 工具。

## 自省

- **资产名前缀要逐家实证**：tag v0.10.0 不代表资产名带 v（zoxide 反例）；pattern 写完先 query --latest 实测再入库，首版想当然全不匹配。
- **替换脚本静默失败同 tail 坑族**：python replace 不带 assert 两处 old 串不精确静默没换（INDEX 的 S004 与 diary 行），靠输出核对抓回；批改必须 assert 命中或跑后核验，与 M006 同根因（工具链吃掉失败信号）。
- **验版本步依赖探测表**：新工具入册三件套是 catalog 条目、toolver 正则、（有需要的）平台字段族；漏正则的表现是「安装后未找到可执行文件或无法读取版本」误报，文件其实在。

## 下一步

D07 切片三（install/deploy 全链适配 agent 与 D08 渠道链）；本批推送后 CI 新 dev 资产到，WSL 侧 status 回写自愈复验。

## 追记：分发方案交底与本机部署位收口

dotfiles 批推送（6ba92ae..75f874e）后：ohmycloud#2 发出（env.ohmygh.com 资产分发准备方案交底：三裁口径、URL 协议无 manifest（信任锚即 ome catalog pin）、版本基线 37 到 43 需刷新、双平台资产先行、P0008 蓝本、ome 侧段式链消费形态）。oma 迁册批（并行会话）交底的本机部署位欠账收口：release 重建更新 `%LOCALAPPDATA%\Programs\ome`，`ome install claude` 幂等 skipped 实证（agent 四家跳过语义在位）、zoxide 幂等绿。

## 再追：基线 ISSUE 三向闭环与实施请求

ohmycloud 在 #5 追加口径更新（P0006 撤、D36 env.ohmygh.com 新口径，请求 1 至 3 维持盼回复）；ome 侧两笔回帖：ohmyagents#4（agent 基线权威转移与五请求重答，oma agents.toml 冻结后现权威在 ome catalog agent 四节，同值无漂移）、ohmyenv-rs#5（回执加实施请求：基线 37 零漂移 git 实证、新增 6 项、UNPINNED 两类排除建议（evergreen 与 uv-git）、POSIX 资产面即平台分列键；用户指令「要它帮你分发」正式请 ohmycloud 把 D36 转实施：建桶挂域、基线刷新 43、手动种子双平台，完成后 ome 侧落地兜底链与断源实测）。用户对 ISSUE 中文排版定调（短段、列表化、留白可换行）已记长期记忆。

## 三追：D36 实施与种子校验抓笔误

ohmycloud 实施快：#5 与 #4 当日双闭（基线 v2 归档 43 节）；env 种子全量下载校验抓出 ome catalog 两处单字符转写笔误（claude win 末位 960E 改 960A、kimi linux 中段 0FA5D1D7 改 0FA5D1F7，均自 oma agents.toml 迁移带入），修 pin 推送（7329271）并回帖请补跑种子 [实证： ohmycloud#2 评论与 GitHub 制品自算值]。种子端「sha 不符拒绝上传」拦截了笔误资产：镜像信任锚即 pin 的双向价值实证（ome 信任镜像、镜像校验 ome）。rust 与 vsbuild 引导器以 latest 段加 sha 边车入镜（evergreen 语义合理变通）；browser-harness 用户裁延后。余量：ohmycloud 补跑通报后 ome 侧 download 兜底链（D07 切片 3 渠道部分）与双端断源实测。

## 四追：D08 渠道链落地与双端断源实测

补跑通报收讫（种子终态 69/69，三方一致）后当日落码：download_asset_with_mirror（官方段失败回落 env.ohmygh.com，仅当 expected_sha256 在位才回落：无锚不产生无校验下载，信任锚即 pin 闭环；双链错误信息带两段）；install 主下载点切换，bootstrap 与 evergreen 引导器（边车锚形态）留第二批。幂等检测安装语义回归双绿（agent PATH 在位与 EnvRoot 版本一致均 skip，用户定调「本地与云端二进制一致就不装、只专注部署检测」不受镜像链影响）。断官方源闸门测试 OME_TEST_MIRROR（官方 URL 用 .invalid TLD 逼回落，断言镜像产物 sha 与 pin 逐字一致）Windows 与 WSL 双端绿 [实证： 当日双端实跑]。测试初版清理用 remove_dir 遇 cache 空子目录 DirectoryNotEmpty 失败（下载与断言已过），改 remove_dir_all；核心链路未受影响。evergreen 边车锚回落与 R004 闸门登记留余量。

## 五追：D07 切片三收口与目标达成

切片 3 尾全收（9c48903）：四家 force 沙盒真装布局全中（kimi win zip 根级 kimi.exe 悬项清零、codex 嵌套 bin、claude/grok 直中，psd1 与 oma 布局数据双源准确）；update 与 daily 对 PATH 在位 agent 纳管跳过（与 install 同判定，真机四家全拦）；verify 定案无 agent 专属维度；R004 登记 OME_TEST_MIRROR；WSL status 自愈复验绿（zoxide/sheldon installed 回写）。D07 四切片全收（切片 4 oma 侧由并行会话交付），PRD D07 推已交付。余量：三节 sha 补 pin、evergreen 边车锚回落（第二批）、M6 挂队列。

## 六追：整仓审查缺陷收口

用户要 review 项目全部，再裁「修」。安装链：pin sha 必须同 tag（M007）、跨发行 sha 只随 lock 写、下载 `.part`（M008）、CDN `--version` 重算 tag。doctor：probe-fail 看文件、死链 `Path::starts_with`（M010）、rust 走 EnvRoot rustup、D13 5s 总超时且网络 WARN 不单独 degraded、TTY 未知状态 fail-closed。POSIX PATH 块多目录（M009）；mac 只认 `mac_exe`；localbin16 去 vault；self update 镜像按通道、替换部署位、catalog 同步保留 pin。PRD D09/D12/D13 已交付；SKILL/README 40 工具。库测 90、cli 25、clippy 绿。余量：zoxide linux_sha256、切片 4、M6。

## 七追：D14 ffmpeg 入册

用户点名 github.com/FFmpeg/FFmpeg 与 ffmpeg.org/download.html。官方明示只提供源码，GitHub 0 个 release。预编译按该页：Windows gyan.dev（GyanD/codexffmpeg essentials 9.0.1）、Linux BtbN n9.0 GPL 静态、mac evermeet 仅 Intel（ome mac 为 ARM，空态）。query 解析与 Windows install 幂等绿，bin 含 ffmpeg/ffprobe/ffplay，status 三态全等 path=true。名录 41。

## 八追：D15 去掉 deploy

用户终裁不要单独 deploy：install 一次完成下载、PATH、注册表与配置；update 是 install 到最新。命令面去掉 download/deploy 子命令，CTA 与 --llms 回到 `ome install`。

## 九追：D16 去掉 daily

用户裁定 daily 同主/跨主分流过重，升级一律 `ome update`。删除子命令与 status 日常更新编排；退出码 2 随命令去掉。install/update 省略工具名即全量（不写 all）。

## 十追：ffmpeg PATH 本会话看不见

HKCU 已有 `D:\ohmyenv\ffmpeg\bin`（status path=true），当前终端仍找不到：写注册表后未广播，且条目已存在时不注入本进程 PATH。补 WM_SETTINGCHANGE 广播与进程 PATH 补齐（M011）。

## 十一追：去掉 package

用户裁定 `package` 命令去除。删除子命令与 `src\package.rs`；分发不再由 ome 承载。

## 十二追：独立仓 D18

用户裁定不再有 ohmypwsh 项目，ome 是独立仓库，只管命令在部署系统上功能完整。D05 M6 与切片 4 取消。活文档（AGENTS/README/ROADMAP/R001/R004/R013/G003）去掉现役依赖；heal 外域只提示不越界；`tests\real.rs` 对照本机 catalog 与 EnvRoot，不再调用 `ohmyenv.ps1`；`import-catalog.ps1` 退役。历史 diary / R012 不改写。本机验收：doctor verdict=degraded exit 0、verify 9 PASS、ffmpeg install skipped、skill/--llms 可跑；cli 28、clippy、四件套绿。

## 十三追：ohmycloud 是分发基建兄弟仓

用户裁定 ohmycloud 是资源分发基建兄弟项目。D19 活文档（AGENTS/README/ROADMAP/SKILL/G003）写明：ome 消费 env.ohmygh.com 镜像兜底，不自建分发面；ohmycloud 源仓只读。

## 十四追：种子清单走 ISSUE

用户裁定种子资源清单用 ISSUE 和 ohmycloud 派任务和对齐。D20：R014 流程加 `.tools\seed-inventory.py`（catalog 41 工具、可入镜 85 对象）。已派 ohmycloud#5（补种 ffmpeg win 与 darwin go/sops，下架 vault/reader，待 sha：zoxide/ffmpeg linux）；本仓跟踪 #8。

## 十五追：ohmycloud#5 通报与镜像闸门

通报 [issuecomment-5571877909](https://github.com/raystyle/ohmycloud/issues/5#issuecomment-5571877909)：差集闭环，终态 85 加 latest 6、182 对象。三处出入已核实：ffmpeg win 入镜（边车=pin）；go/sops mac 本就在镜（ome 视角滞后，M012）；vault/reader 已 404；zoxide linux 误入已删（对方缺 sha 闸门只卡 mac，已泛化）。本仓 `OME_TEST_MIRROR=1` 两测绿（zoxide win、rmux mac 回落 sha=pin）；ffmpeg 边车与 pin `FEC81AE0…` 一致。

## 十六追：linux sha 回填并派补种

zoxide linux `2D93385B99F3E82C…B1AE`、ffmpeg linux `1DE6E23E9FD11CD8…8113`：官方资产独立哈希，与 GitHub release digest 逐字一致。域面 HEAD 404 后派 [ohmycloud#7](https://github.com/raystyle/ohmycloud/issues/7)。catalog 可入镜 87 对象，缺 sha 清单空。

## 十七追：三面对齐派单

用户要给 ohmycloud 发 ISSUE，对齐分发、更新、安装 ome 和 ome 管理的软件。D21 已派 [ohmycloud#8](https://github.com/raystyle/ohmycloud/issues/8)（姊妹 #6 是 oma 加四家 agent）。本仓约束写进 ISSUE：入口只在 ome，镜像只做种子，官方失败才回落。
