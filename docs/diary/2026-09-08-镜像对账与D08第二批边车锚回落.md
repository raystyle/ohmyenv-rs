# 2026-09-08 镜像对账与 D08 第二批边车锚回落

## 镜像对账五点回报

- 镜像方回执：v2.4 终态可入镜 87 加 latest 段 6，台账 93/93，pending_sha 空。
- 本侧独立实证：zoxide linux 0.10.0、ffmpeg win 9.0.1、ffmpeg linux 9.0 三资产镜像 HEAD 200。
- 回报五点落 `C:\Users\ray\AppData\Local\Temp\ome-dist-sync.md`（用户指定路径，镜像方直接读）。
- 对账结论：catalog 单提对齐 GitHub（9581b6d）；stable/dev 拆分等 v* tag 封版（ROADMAP 阶段七待定）；rclone（ohmyenv-rs#9）等用户裁决。

## 批次收口用户裁决 catalog 单提

- 9581b6d feat: ffmpeg 入册与 zoxide linux_sha256 回填（仅 tools.toml，镜像消费源）。
- d2a9a33 feat: D15..D17 命令面收敛（install 全包、daily/package 退役、ffmpeg toolver）。
- 2dea0dc docs: D15..D21 四原语与命令面文档同步。
- dfc588f docs: R014 落参考、R004/R013 更新、seed-inventory 入 .tools。
- 022b36b chore: gitignore 补 .oma。
- 门禁先行：cargo test / fmt / clippy / rumdl / md 三扫描全绿后才提交；推送 5f66801..022b36b。

## D08 第二批落地 ohmyenv-rs#7

- download.rs：`download_latest_with_sidecar`（官方失败回落 `{tool}/latest`，先取 `.sha256` 边车为锚再下资产，边车取不到即拒绝无校验下载）；`mirror_sidecar_sha` 自 selfupdate 迁入统一权威；显式 URL 形态供单测注入不可达地址。
- rustup.rs / vsbuild.rs：引导器下载换 latest 段回落链。
- 测试：单测（边车解析、双断拒绝且不留未校验资产）加 OME_TEST_MIRROR gated 6/6 绿（rmux mac、zoxide win/linux、rust、vsbuild、ffmpeg HEAD 在位；oracle 为测内独立取边车首 token）。
- 文档同步：README/SKILL/AGENTS 锚口径（pin 或 latest 段边车）、R004 镜像兜底行、INDEX、PRD D08、CHANGELOG、TODO、GOAL。

## 发现与派单

- 镜像缺 `ome/dev` 段：selfupdate dev 通道（默认）断源回落 404，现只有 ome/latest（放 dev 资产）；已派 ohmycloud#9（补三资产加边车，沙滚，封版拆分两段各滚）。
- ohmyenv-rs#7 留收尾项：ohmycloud#9 入镜后补 self update 断源用例再关单。

## D08 第二批收官与 herdr 升级踩点午后

- ohmycloud#9 闭环（三资产加三边车 HEAD 200，两段同源实证）；补 self update 断源锚链用例（断官方 API 复刻：锚取 ome/dev 边车、资产带锚走镜像段），OME_TEST_MIRROR 7/7 绿；关 ohmyenv-rs#7。
- herdr 0.9.0 升级踩点（catalog 三平台 pin v0.8.2，上游 latest v0.9.0）：
  - 旧部署 ome 二进制（09-07 17:15）早于 pin/sha 同 tag 防护（5f66801 19:29），update 用旧 pin sha 校验新包必失败；`ome self update`（BD367811）后防护生效、下载通过。
  - install 删旧目录撞运行中服务器 exe 锁与 `conpty/` 运行态，面板内 `herdr update --handoff` 亦被拒（须 detach 后面板外跑）；升级剧本交用户面板外执行，`ome update herdr` 补 pin 加 sha 回填后派镜像种子。
- 镜像二轮对账四点回报（push 计划、herdr 种子口径、#8/#6 待办面、零差集）落 Temp 供镜像方读取；两轮 docs push 各滚一档 dev（win 25d49e5f 加 b5d7442f，unix 不变），均按口径通知。
- ohmyenv-rs#6 关单：四家 `ome install` 幂等实证（全 skipped exit 0，版本探测如实），ohmycloud#6 的 ome 侧挂钩清空。

## 五端验收与镜像链加固

- 用户裁五端验收（ome 最新版安装加更新，ohmycloud 执行）：矩阵定稿本机 win / lan-win / lan-linux / lan-mac / wsl 裸装（lan-pve 不测、云按需不入）；修正两处口径（win 锚已滚三档改动态锚判据；`--version` 恒 0.1.0 仅活性、档位鉴定用 exe sha 对边车）；`--stable` 不入（未封版回落 latest 会装 dev 产物）。
- B 面补运行开关 `OME_MIRROR=1`（c871e35）：self update 跳官方 API 直取边车锚（OME_TEST_MIRROR 是测试闸门不是运行开关，远端断源不可构造故落此）；候选档三 sha 报镜像重播两段，push 冻结至验收。
- 验收通报**全过**：A 面本机 omc tool install（首次被锚校验正确拦下 CF 边缘陈旧缓存，锚纪律起效）、wsl 裸装、lan 三端锚逐字吻合；B 面五端过，终态锚三平台全等（win f3c41009 / linux 9aaed1f2 / mac 9df6db15）。
- CF 陈旧缓存跟进（解冻第一笔）：镜像侧修自己客户端 query 击穿后，ome 镜像链同样暴露在该缓存语义下；统一加击穿 query（边车 `?t=` 时间戳回源、资产 `?v=<锚>`，锚变键变、锚同对象必一致），gated 7/7 复验过（167.8s，回源变慢即击穿旁证）。

## rclone 入册 D22 傍晚用户继续推进队列

- ohmyenv-rs#9（ohmycloud 请求：env-seed 上传链切 R2 S3 直传）：catalog 三平台节（`SHA256SUMS` 统一清单锚即 rmux 同款 sums_asset；win zip 展平单根目录、linux/mac zip 带版本子目录走 zip-bin 取单二进制）。
- 坑：toolver `version_pattern` 表外工具探测直接 None（`?` 短路无兜底），新工具入册必须同步补表项（D14「toolver 补两家正则」既知模式，本次漏带）；补 `rclone\s+v(\d+\.\d+\.\d+)` 后装后校验过。另：改的是 `target/debug` 新构建，PATH 上部署 ome 是旧档，验证须跑新构建二进制。
- win 真机：query --latest 过 sums 链、pin 1.75.1、install 绿（缓存命中 sha 一致、回填 sha、PATH 注册）；sha `200EB602…` 与官方 SHA256SUMS 逐字一致（独立复核）。linux/mac pin 按官方清单直接回填（zoxide 同款先例）。
- 计数 41 改 42（AGENTS 两处、README 三处含类表 18 改 19、SKILL、INDEX）；PRD D22 登记；测试全绿无 golden 破面。

## herdr pin 先行收尾晚间

- 「继续」再推进 herdr：盘上仍 0.8.2（用户未跑面板外剧本）。裁决取 pin 先行：win 四键升 v0.9.0（sha `B4508C44…` 双源核验：GitHub digest 与 ome 昨日缓存实测一致；rclone 顺手当 sha256sum 用）；盘上二进制不动，与运行中服务器保持 0.8.2 一致，避免新客户端旧服务器协议错配；status 如实显漂移（locked 0.9.0 / installed 0.8.2）。
- 余一步归用户：停服后 `ome update herdr`（pin 驱动解析、缓存命中可离线装）再起 herdr；镜像侧 herdr/0.9.0 win 种子三键随派、0.8.2 段保留（linux/mac pin 仍 0.8.2 两台机器回落引用）。

## D23 双工具重入册夜间

- 用户指令「增加 browser-harness 与 reader_rs」：撤 2026-09-07 暂不接管裁，恢复入册（git 历史里旧节是现成起点）。
- reader 现释放 v0.6.0 三平台资产加 `.sha256` 后缀边车：旧节字段照搬升版本，win 真机幂等绿（撤册前 0.6.0 装机还在）；toolver reader 正则撤册时未删正好复用。
- browser-harness 仓已重写 TS（browser-harness-ts 0.5.1，release 制品是 npm 包 tgz，无二进制），旧 uv-git 通道作废；落 **npm-tgz 新安装通道**：tgz 过锚下载（可镜像回落）后 `npm install -g`，bin 名 bh 与工具名不同。
- 三个 Windows 机制坑顺修：npm shim 无 .exe（find_on_path 补 .cmd/.bat 候选；installed_version 对脚本 shim 经 cmd /c 拉起）；npm 全局 bin 无静态路径（exe_path 与 status 三态走 PATH 现查，status 分支 bin 名优先于工具名：曾因用工具名误中 uv 时代残留 browser-harness.exe 0.6.12）；register_bin 对 npm-tgz 不注册 EnvRoot（防死链，误注册的 D:\ohmyenv\bh 已从注册表清除）。
- 遗留知会：`D:\ohmyenv\uv-tools\bin\browser-harness.exe`（0.6.12，uv 时代残留）与新 bh 并存，归用户裁决是否 `uv tool uninstall browser-harness`。

## D24 lightpanda 入册并本机先装深夜

- 用户指令「增加 lightpanda-io/browser」加裁「先本机安装」。上游形态：最新稳定 0.4.0（tag 无 v 前缀），**无 Windows 构建**，linux/mac 裸二进制（163MB 级）加 deb，无 sums；`releases/latest` 被 nightly 滚动 tag 占据（latest 解析会拿到 nightly，升级须显式 `--tag`）。
- 本机先装落 WSL：curl 直下 x86_64-linux 落 ~/.local/bin，sha 与 GitHub digest 逐字一致；`--version` 是 UnknownCommand（结构化日志 fatal），版本走 `version` 子命令出裸 0.4.0（toolver 走 zig/go 同款子命令形态）。
- 入册：win 空态（sheldon 反向先例），linux/mac copy 裸二进制，pin 0.4.0 双平台 digest 锚；计数 44 改 45。

## 收尾杂项与 herdr 0.9.0 落盘

- uv 残留清理（用户裁删）：`uv tool uninstall browser-harness`，0.6.12 旧 Python 版退场；bh 探测现报 0.5.2（w1 开发会话树内版本，pin 仍 0.5.1 如实漂移）。
- herdr 0.9.0 落盘（用户催办）：面板内 `herdr update --handoff` 被拒后，经 schtask 以会话外上下文跑通厂商更新器；但其安装器落在 `C:\Users\ray\AppData\Local\Programs\Herdr\bin` 并将该目录提到 PATH 最前（与 ome 装位 `D:\ohmyenv\herdr` 双轨），活交接未执行（提示 Restart sessions），服务器仍 0.8.2。
- 待用户重启后收口三步：新终端起 herdr（0.9.0 服务器）；`ome update herdr` 缓存命中把 ome 装位刷 0.9.0；PATH 收敛回单轨（撤厂商目录优先级）加 `herdr integration install claude` 刷新种子。

## herdr 收口完成午夜前

- 用户重启切 0.9.0（server 与 client 双 0.9.0、protocol 22、会话存活，镜像方实测对齐）。厂商安装器当时已顺带把 ome 装位升至 0.9.0，`ome update herdr` 幂等确认零回写（pin 与 sha 先行在库的红利）。
- PATH 收敛：撤 `C:\Users\ray\AppData\Local\Programs\Herdr\bin` 注册表优先级，回 ome 装位单轨；厂商目录文件留盘（运行中服务器映像锁定，下次重启可删）。`herdr integration install claude` 刷 hook 与 settings 种子。
- 终态：status 三态全绿（locked=installed=0.9.0、path=true、exe 指 `D:\ohmyenv\herdr\herdr.exe`）；镜像 win 双档在位（0.9.0 与 0.8.2 各按 pin 与 lock 引用）；linux/mac pin 0.8.2 随对应机器升级再滚。今日队列全清，挂账仅 build.yml docs 路径过滤待用户裁决。

## D25 skill 逐工具自适应引导深夜二

- 用户需求「查询本机已装工具与运行时、引导 Agent 用途与注意事项（env/path/版本）」，AskUserQuestion 两裁：扩展 ome skill 面、全 45 工具起稿；随后补裁自适应（结合安装路径、环境变量、数据目录实测生成）。
- 落法：catalog 增 desc/guide_env/guide_dirs/guide_notes 四字段（.tools/inject-guide-d25.py 幂等注入 45 节；TOML 多行串反斜杠转义坑一次）；render_skill 重构为逐工具块（三态标记、exe 实测、env 键实测用户级优先、目录实测在位与否、注意事项逐行）；凭据类键（TOKEN/KEY/SECRET/PASSWORD）只显在否，与 doctor 纪律同源。
- 顺修既有 bug：cmd_skill 先渲染自适应文本再被 deploy_skill 用静态版覆盖落盘，数据目录件恒静态；改 write_skill 落自适应文本（静态骨架仅 init 兜底）。
- 本机实证：rust 块 env 四键真实值、GH_TOKEN 已设不显值、lightpanda/shellcheck 空态如实标；门禁全绿。
