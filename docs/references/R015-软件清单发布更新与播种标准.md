# R015：软件清单发布更新与播种标准

> 角色：现役标准。ark 软件清单（`catalog\tools.toml`）的**发布、更新**与**软件资产播种**三流程的唯一流程权威；字段与 pin 语义在 R001，跨仓协调与段域在 R014，签名方案研究在 S006，测试闸门在 R004 三.6。2026-09-10 定档（D32/D33/D34 收口后成文，D35）。

## 一、角色与信任链

| 角色 | 承担 |
| --- | --- |
| 仓库 `catalog\tools.toml`（D37 已退役） | 本仓不再持有权威件；入册走 omc 数据面（其仓 `catalog/tools.toml` 加 catalog-lint 四规则门），本仓只保留 R001 格式契约与 `tests/fixtures` |
| 云端 `ark/catalog/`（env.ohmygh.com） | 运行态权威：清单本体、`.sha256` 边车、`.minisig` 签名件三件套。D41 起主键 `ark/catalog/`、端上兼容读 `ome/catalog/`（键读序主先兼容后；停 ome/ 键判据为存量机水位清零） |
| `.sha256` 边车 | 传输完整性锚（种子端自官方资产或清单自算即锚） |
| `.minisig` 签名 | 来源真实性锚（Ed25519；公钥内嵌 ark 二进制，当前 `FB93BFD3788C2316`；轮换走内嵌集合过渡） |
| 私钥 | 只在开发机 `~/.config/ome/` 与 GitHub Secret `CATALOG_SIGNING_KEY`，永不入仓。D41 裁定：密钥位**保留不迁移**（密钥材料零扰动，与 EnvRoot 物理目录同类；后续轮换时再议） |

## 二、清单发布：数据面到云端

权威与发布运营在 ohmycloud（D37 终版）：权威 `catalog/tools.toml` 落 ohmycloud 仓（初值自本仓复制，遵循本仓 R001 格式契约），catalog-lint 四规则门（vitest 形态）进其 CI 首步，catalog-seed 流水签名发布云端三件套并播种资产域（批 2 移植 seed.py 域面 diff 逻辑，其仓权威单源）。

ark 侧义务收缩为：改消费面（新 extract 类型或新字段）时以 R001 契约 herdr 知会 omc 同步 lint 与数据；端上验收 `ark catalog sync` 后 `signature=valid`。

## 三、清单更新：云端到端

| 通道 | 语义 |
| --- | --- |
| `ark catalog sync` | 显式刷新，不受 TTL 与 `ARK_OFFLINE` 限制；也是修复通道 |
| TTL 自动刷新 | 仅用户数据副本；默认 24h（`ARK_CATALOG_TTL` 可改，0 关；`ARK_OFFLINE=1` 等效关；旧名 `OME_*` 读回）；单次 5s 探活加 `.last-sync` 退避标记 |
| 裸端自举 | 四级解析全 miss：只走云端镜像同款三重门（键读序 ark 主先、ome 兼容回落）；镜像不可达即 fail-closed 报错（提示网络与 `ARK_CATALOG` 出路），无免验兜底 |

- **三重门（落位前缺一不可）**：边车 sha 锚比对、`Catalog::load` 解析、内嵌公钥验签；任一不过拒收。
- **落位纪律**：临时名随目标派生（`tools.toml.tmp` 与 `tools.toml.minisig.tmp`）；先清单后签名件。
- **加载前巡检**：invalid 阻断全部非 catalog 命令（含 self update，防被篡改面自我升级）；missing 仅用户数据副本告警；catalog 子命令整组豁免保自愈。
- **本地回写**：pin/update 改写即撤签名（内容已变，不留假凭证）；下次 sync 恢复。
- **升级 runbook**：跨版本 self update 后带一次 `ark catalog sync`（旧版孤儿签名为一次性迁移伤痕，0.3.0 起条件化撤签名防复发）。

## 四、软件播种：资产到云端

| 路线 | 触发 | 段域 |
| --- | --- | --- |
| A 自产 | push main、push v* tag、workflow_dispatch | main 灌 `ark/dev` 加 `ome/dev` 双沙滚段；v* tag 灌双 stable 段（D41 B 双写：ark/ 段配 `ark-*` 主名资产、ome/ 段配 `ome-*` 兼容名，同 job 两族各跑一次 seed.py；段名与 self update 通道同名；`ome/latest` 已退役；停 ome/ 段与撤 `ome-*` 资产名判据为存量机水位清零） |
| B catalog | ohmycloud catalog-seed（dispatch 加 6h 定时；本仓 seed-mirror 已整撤） | 其仓权威清单的软件资产 `/<tool>/<version>/<asset>` 加边车；`ark/catalog/` 三件套（清单加边车加 `.minisig`；D41 起主键，兼容键 `ome/catalog/` 由 omc 铺段前维持供给，端上键读序主先兼容后） |

- 无 sha 不入镜（pending 队列）；边车自算即锚；rclone NO_CHECK_BUCKET 必带；Cache-Control 60s 加消费侧 `?v=`/`?t=` 击穿双保险。
- 消费侧：官方渠道优先、镜像回落仅当有锚（catalog pin 或 latest 边车）；selfupdate 分通道对应段；**查询段回落（D38）**：pin 四键齐且 GitHub API 失败（私有仓匿名 404、限流、断网）时直拼镜像资产域 URL 过 pin 锚安装，`ARK_MIRROR=1` 强制镜像（解析面真跳过 API，selfupdate 同名开关语义扩展；旧名 `OME_MIRROR` 读回）。

## 五、管辖边界

2026-09-10 用户两裁递进定稿（终版：完全解耦）：

| 域 | 管辖 | 内容 |
| --- | --- | --- |
| ark 自身 | ark 仓全权 | 代码与功能更新、发版（tag、CI、双写自产段播种）、二进制自更新（self update 三通道） |
| 软件清单数据 | omc | **权威 tools.toml（条目与 pin 维护、版本升级决策）、catalog_lint 发布门**、签名与三件套发布、软件资产播种（A 形态） |
| ark 消费逻辑 | ark 仓 | 诊断逻辑（doctor：系统 / 依赖两层加 check 节）、安装与部署逻辑（install / update / query / status / verify / heal / catalog 子命令）、清单**格式契约**（R001 schema 与消费代码）、端上三重门与巡检 |

ark 与清单数据权威**完全解耦**：ark 仓不再持有权威 `catalog\tools.toml`（迁移后退役为测试夹具），端上清单一律云端拉取（自举与 sync 与 TTL）；ark 改消费面（新 extract 类型、新字段）时以 R001 契约知会 omc 同步 lint 与数据。

承接形态已定（2026-09-10 omc 回执）：**B，ohmycloud 接管签名与播种流水**。ohmycloud 侧建 seed 流水（拉本仓 catalog 唯一 pin 源、签名、推 `env.ohmygh.com/ome/catalog/` 三件套），`.tools/catalog-sign` 工具与公钥由 omc 侧自行复制入其仓（本仓对其只读铁律），`CATALOG_SIGNING_KEY` Secret 值由用户灌入 ohmycloud（私钥只在本机，不经第三方面）；本仓 R001 checklist 与 catalog_lint 机检仍为发布门。**双轨期**：本仓 seed-mirror 流水不撤保供给连续，对方流水首发并经本仓验签验证后，本仓撤 Secret 与签名步（路线 A 自产段不涉移交）。进展与收口（2026-09-10）：ohmycloud 0501e35 复制工具与公钥（key id 同源核验）并建 catalog-seed 流水（dispatch 加 6h 定时，缺 Secret 显式失败）；用户灌三枚 Secret（签名钥文件直灌加 R2 凭据 stdin 灌，零泄露）后首跑 success（run 34483609407），云端三件套由其流水重灌新签；本仓独立 `catalog sync` 验签 `signature=valid`，撤退完成：本仓 `CATALOG_SIGNING_KEY` Secret 已删、seed-mirror 签名步已撤、seed.py 摘 catalog 本体上传（防双轨竞态出锚对签失配窗口态），软件资产域定稿 A 形态（2026-09-10 omc 回执）：catalog-seed 扩展接管资产播种（pin 域面 diff、无 sha 不入镜、边车自算即锚移植其仓，拉本仓 pin 源单源）；本仓 seed-mirror 双轨续跑至对方资产域首发加对账绿，届时撤整条只留 `seed.py --plan` 对账面。

## 六、例外与挂起

- 开发面（仓库 cwd 与 `OME_CATALOG` 指定面）不入签名巡检：信任自担，开发流程零干扰。
- 自举闭环实证（2026-09-11，omc 打包封板安装全链）：omc 以 npm-tgz 入册（私有仓 GitHub 匿名 404 场景）：lan-mac `ome install omc` 经 D38 回落镜像直装过 pin 锚、npm 全局装成功、装后 doctor 七检查全通；数据面两小修自愈（probe 容忍 v 前缀、guide 补 npm allow-scripts 提示）零改 ome 代码。R015 体系至此含 omc 自身分发的完整自举。
- `ome/latest` 旧段已下架（2026-09-11 ohmycloud 执行，env 桶余 catalog/dev/stable 三段），S017 O1 至 O7 双仓闭环（ohmycloud cb7a5cb 终态文档）。
- 挂起：回滚与重放防护（清单内 generated_at 加单调 seq，拒收更旧，S006 候选 B）：防护场景定义为「旧但签名有效的清单对重放」与「镜像桶对象回滚」，解法待排期（用户裁决）。
